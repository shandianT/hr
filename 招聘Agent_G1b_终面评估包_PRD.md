# 招聘 Agent G1b PRD：多轮串联与终面评估包

版本：v0.1  
日期：2026-08-10  
状态：Gate 0 评审稿  
产品边界：G1a 轮次完成件 → 多轮依赖与面前追问 → 终面评估包 → 权威审阅任务  
发布结论：真实数据、A0/A1、外部发送和工程发布仍为 No-go

相关材料：

- [产品落地总方案](./招聘Agent产品落地总方案.md)
- [领域语言](./CONTEXT.md)
- [领域与事件规格](./招聘Agent_领域与事件规格.md)
- [G1a MVP PRD](./招聘Agent_G1_MVP_PRD.md)
- [ADR-0005：终面评估包是不可变编译物](./docs/adr/0005-final-assessment-package-is-an-immutable-compilation.md)
- [ADR-0006：面前追问简报采用分阶段披露](./docs/adr/0006-interview-brief-uses-staged-disclosure.md)

## 1. 要解决的问题

G1a 可以把一轮面试加工成可信的轮次完成件，但多轮招聘仍有四个断点：

1. 前一轮结束后，下一轮是否满足条件、由谁负责和何时启动仍靠 HR 搬运。
2. 下一轮面试官不知道前面还缺什么证据，只能重复提问或被前轮结论锚定。
3. 终面时，已确认评价、人工决定、豁免、缺项和冲突散落在不同页面，HR 需要手工拼报告。
4. 轮次材料被修订或删除后，已拼好的终面材料可能继续被使用，无法证明决策人当时看到的依据。

本期要证明：

> Agent 能在不替代任何面试官或最终决策人的前提下，按钉住的面试计划持续激活后续轮次，为下一轮生成低偏见的追问简报，并把当前有效的轮次完成件编译成一个可追溯、可失效、无招聘建议的终面评估包，自动放入最终招聘决策人的权威待办。

## 2. 成功定义

G1b 成功需要同时满足：

- 每个被纳入汇总的轮次都有当前有效的轮次完成件，或有权限的人类轮次豁免。
- 下一轮只在依赖满足且前轮人类决定允许时被激活，不依赖 HR 主动点击推动。
- 面前追问简报只展示本轮需要补的证据和合规问题，不提前暴露前轮总分、整体结论或推进决定。
- 终面包按评分卡维度呈现证据、信息不足、跨轮问题和豁免，不把缺失包装成负面结论。
- Agent 不产生录用、淘汰、排名、推进或单一综合人格/能力分。
- 任一当前输入被替代、撤回、删除或过期，旧包与旧审阅任务立即不可继续用于新的最终决定。
- 重复事件、并发编译、旧链接和通知重试不产生第二个当前包或重复待办。
- 全过程可审计、可暂停、可人工接管，并遵守字段级权限和权利请求。

北极星子指标：**有效终面评估就绪率**。  
定义：在目标时限内形成当前有效终面评估包并创建权威审阅任务、且无计划外 HR 搬运、无未处理阻断项、无安全/合规事件的合格申请案件数 ÷ 所有进入 G1b 汇总范围的合格申请案件数。

该指标不统计最终录用结果，也不把“与最终决策一致”当作模型正确率。

## 3. 范围

### 3.1 本期包含

- 一个内部组织、1–2 个试点岗位，复用 G1a 的租户/权限/审计边界。
- 一个钉住的面试计划版本；支持 2–5 个逻辑轮次、顺序依赖和少量并行依赖。
- 必需、可选和条件轮次的显式定义；依赖图必须无环。
- 前轮当前轮次完成件产生后，按人类轮次决定和计划规则自动激活下一轮。
- 重复轮次使用新 Round ID 和来源关系，不覆盖原轮次。
- 面前追问简报的生成、校验、版本、权限、产品内待办和安全通知。
- 轮次完成件、豁免、证据引用的输入清单和去重。
- 按评分卡维度进行跨轮汇总；显式展示信息不足、差异、问题候选和豁免影响。
- 终面评估包编译、校验、版本、不可变归档、失效和重编译。
- 最终招聘决策人的产品内权威审阅任务；外部 IM/邮件只作最小通知。
- 控制塔中的多轮完整性、当前包、异常、SLA、审计、质量与成本事件。

### 3.2 本期不包含

- 自动作出、预填或推荐最终录用/淘汰决定。
- Offer、背调、薪酬审批、拒绝信和最终决定后的候选人沟通。
- 将前轮分数、推进决定或身份化意见默认展示给下一轮面试官。
- 用面试数据对候选人进行情绪、性格、诚信、健康、婚育或其他禁用推断。
- 自动改变岗位画像、评分卡、轮次权重或面试计划。
- 简历收件、匹配、部门卡片、候选人协调和日历生产自动化；它们属于 G2。
- 结果回流画像、Offer/入职/绩效观察和画像变更提案；它们属于 G3。
- 任意可编程工作流、无限轮次或无约束的条件表达式。
- 跨租户、多客户配置、ATS 回写、计费或 OEM。

### 3.3 合格输入前置条件

一个申请案件只有同时满足以下条件才进入 G1b：

1. `ApplicationCase.stage=INTERVIEWING`，案件未关闭、撤回、暂停或进入删除。
2. 当前面试计划版本已钉住，依赖图无环，必需/可选/条件语义明确。
3. 岗位画像和评分卡版本已钉住；版本迁移有授权记录和影响清单。
4. 至少一个轮次完成件来自 G1a 的当前已确认评价和人类轮次决定。
5. 下一轮确认责任人、指定面试官和最终招聘决策人可解析且权限有效。
6. 证据、评价和决定没有处于删除、超保留期或已被 supersede 状态。
7. 面前简报与终面包的字段披露、禁用特征和访问策略已经过批准。

不满足时不得“先汇总再补”，而应停在明确的缺项或异常包。

## 4. 用户与权限

| 角色 | 可以做 | 不能做 |
|---|---|---|
| 指定面试官 | 查看自己下一轮的面前追问简报；编辑/忽略建议；完成独立评价 | 在独立评价前查看前轮总分、决定或身份化意见；查看无关轮次原始材料 |
| 评价确认人 | 确认当前轮评价；处理本轮多人意见 | 替其他轮次确认；把 Agent 摘要当最终决定 |
| 最终招聘决策人 | 查看当前有效终面评估包、缺项/问题/豁免及源证据；作最终决定（决定动作不在本期） | 使用已失效包或旧链接提交新决定；让 Agent 代选 |
| 用人部门负责人 | 确认面试计划、评分卡、豁免权限和问题严重度策略 | 无审计修改已确认评价或历史计划版本 |
| 招聘 HR | 查看多轮进度、Owner、SLA、异常和包版本；处理路由/责任/恢复 | 手工拼一个绕过完整性门的“终面包”；无痕改评价或决定 |
| 招聘运营管理员 | 配置计划模板、简报模板、问题策略、SLA 和通知策略 | 自动更改在途案件的钉住版本 |
| 隐私/安全管理员 | 查看披露、访问、删除、供应商和审计事实 | 修改候选人业务评价或替人解决跨轮问题 |
| 质量标注员 | 在隔离抽检任务中标注简报/汇总错误 | 批量搜索或导出全部候选人材料 |
| Agent | 识别证据缺口、生成问题建议、编译摘要和提出跨轮问题候选 | 激活不满足依赖的轮次；披露前轮决定；解决冲突；作招聘决定；改画像/评分卡/计划 |

权限同时受租户、部门、岗位需求、申请案件、轮次、数据类型、处理目的、披露阶段和时间范围约束。

## 5. 产品组成

| 产品模块 | 核心产物 | 价值 |
|---|---|---|
| 多轮编排器 | 当前计划图、依赖、Round Completion Record、下一动作 | 前轮人类决定后自动继续，不靠 HR 搬状态 |
| 面前追问简报 | 当前轮待验证维度、建议问题、理由、版本和受众 | 减少重复提问，同时控制锚定和信息扩散 |
| 跨轮问题中心 | 缺项、问题候选、严重度、处置与责任人 | 不把矛盾和豁免藏在摘要里 |
| 终面评估编译器 | Input Manifest、按维度摘要、证据、问题、豁免、版本 hash | 形成可复现、可失效的决策材料 |
| 最终审阅任务 | 产品内权威待办、安全深链、通知回执 | 把当前材料真正摆到最终决策人桌面 |

## 6. 主流程

| 步骤 | 触发与系统动作 | 产物 | 状态/权限要求 |
|---|---|---|---|
| 1 | 当前轮形成轮次完成件 | Round Completion Record | 已确认评价 + 当前人类轮次决定 + 证据版本 |
| 2 | 编排器加载当前面试计划并计算依赖 | 可激活步骤或缺项 | 计划版本当前、图无环、决定允许继续 |
| 3 | 控制面激活下一轮或新建重复轮次 | 新 Round | 不能覆盖原轮；操作幂等、带 version/epoch |
| 4 | 汇总当前证据覆盖与下一轮评分卡目标 | Brief Input Manifest | 只用当前有效输入，不含禁用字段 |
| 5 | Agent 生成面前追问简报草稿 | Interview Brief Draft | 每条问题有维度/缺口/理由；无前轮总分/决定 |
| 6 | 运行权限、分阶段披露、禁用特征与诱导性校验 | 可发布简报或异常包 | 失败不得静默删字段后继续 |
| 7 | 创建指定面试官产品待办并安全通知 | Current Interview Brief Task | 收件人、轮次、版本和业务幂等键当前 |
| 8 | 后续轮次重复完成；编排器重算必需轮次 | Current Completion Set | 重复/并行/豁免均显式记录 |
| 9 | 全部必需输入满足后冻结 Final Input Manifest | 输入清单 | 仅当前完成件和有效豁免；证据去重 |
| 10 | Agent 按评分卡维度编译摘要并提出问题候选 | Package Draft | 事实/判断/不足/问题分开，关键 claim 有证据 |
| 11 | 控制面校验完整性、版本、问题严重度、决定字段和权限 | Valid Package Version 或异常 | P0/P1 未处理、缺项或决定字段均阻断 |
| 12 | 创建最终招聘决策人产品内权威审阅任务 | Final Assessment Task | 引用唯一当前包；不预选决定 |
| 13 | 将 Case 标为 `FINAL_ASSESSMENT_READY` | 终面评估就绪 | Package 与 Task 当前有效；仍未作最终决定 |
| 14 | 任一输入变化或权利请求到达 | Package Invalidated | 旧任务撤回/标旧，Case 回 INTERVIEWING，重走完整性门 |

## 7. 产品界面

### 7.1 多轮编排视图

必须显示：

- 当前面试计划名称、版本、锁定时间和授权人。
- Round 节点、必需/可选/条件属性、前置依赖、当前状态和责任人。
- 每轮当前完成件：确认评价版本、人工决定版本、证据 hash 和完成时间。
- 重复轮次的来源关系，不把它伪装成原轮修改。
- 豁免的范围、授权人、原因、时间和证据影响。
- 下一自动动作、唤醒条件、SLA、阻断项和异常包。

不能提供“直接编辑 RoundState”或“直接把案件设为终面就绪”的入口。

### 7.2 面前追问简报

每条建议问题显示：

- 本轮评分卡维度或证据缺口。
- 建议问题与建议使用场景。
- 为什么需要验证；只显示最小必要的去身份化上下文。
- 风险提示：低置信、可能诱导、敏感话题或已重复提问。
- 接受、编辑、忽略、报告问题。

独立评价提交前默认隐藏：前轮总分、整体评价、推进/淘汰决定、面试官姓名化意见和完整逐字稿。系统必须显著说明简报是建议，不是面试脚本。

### 7.3 跨轮问题中心

每个问题卡包含：

- 问题类型：缺项、表述冲突候选、适用范围差异、证据复用、版本失效。
- 相关轮次、维度、当前 claim 和证据指针。
- Agent 只说明“为何值得复核”，不判断哪一轮正确。
- 严重度、是否阻断、Owner、截止时间和允许的解决选项。
- 解决记录：补证、澄清适用范围、承认信息不足、授权非阻断或 supersede 输入。

### 7.4 终面评估工作台

页面结构：

1. 包头：候选人、岗位需求、计划/画像/评分卡版本、包版本、生成与有效状态。
2. 完整性：必需轮次、完成件、豁免、缺项和阻断问题。
3. 按评分卡维度汇总：已验证事实、证据支持判断、反证/差异、信息不足。
4. 轮次时间线：每轮确认版、人工决定和修改/失效历史。
5. 跨轮问题与非阻断标记，不允许折叠后默认隐藏。
6. Evidence Manifest：来源 Round、Evaluation、Evidence Atom、版本和 locator。
7. 审计与有效性：当前输入 hash、最后重验时间、旧包历史。

页面不得出现 Agent 的 HIRE/NO_HIRE、推进建议、候选人排名或单一总体人格/能力分。

### 7.5 最终审阅任务

- 产品内任务是权威入口；IM/邮件只发最小信息和安全深链。
- 卡片显示当前包版本、完整性、阻断项数和“仍需人类最终决定”。
- 不显示预选的录用/淘汰按钮状态。
- 打开旧链接时必须显示“已失效”，只能查看历史，不能用于新决定。
- 通知失败进入异常和升级，不通过重复创建任务解决。

## 8. 功能需求

### 8.1 面试计划、依赖与轮次激活

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-101 | 每个案件进入 G1b 前必须钉住一个不可变面试计划版本 | 计划包含 Round Step、必需性、前置依赖和角色；在途不自动跟随模板更新 |
| FR-102 | 计划必须显式区分必需、可选和条件轮次，且依赖图无环 | 存在环或未定义条件时不得启动编排 |
| FR-103 | 下一轮只在所有适用前置完成件/豁免有效，且当前人工轮次决定允许继续时激活 | Agent 不能把分数或摘要转换为激活决定 |
| FR-104 | 重复轮次必须创建新 Round ID 并引用来源轮次/原因 | 原轮评价、决定和历史状态不得被覆盖 |
| FR-105 | 面试计划修订必须由有权限的人批准并产生新版本、影响清单和下游失效 | 不相关历史保留；受影响 Brief/Package/Task 全部重验 |
| FR-106 | Round 激活、豁免、重复和计划迁移全部走确定性命令门 | 页面、模型或后台脚本不能直接写 RoundState；需幂等、version、epoch |

### 8.2 面前追问简报

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-107 | 只有当前已激活 Round 和当前指定面试官可以形成/读取简报 | 取消、改派、暂停或旧 Round 的简报不得继续分发 |
| FR-108 | 简报输入只使用当前评分卡、本轮未覆盖维度和允许使用的当前先前证据 | 被 supersede、删除、超期或跨目的材料不得进入 |
| FR-109 | 面试官提交独立评价前，简报默认不得包含前轮总分、整体结论、决定或身份化意见 | Schema、披露策略和发布校验三层阻断 |
| FR-110 | 每条建议问题必须引用评分卡维度或证据缺口，并记录理由和生成版本 | 无业务目的或无来源的“灵感问题”不得发布 |
| FR-111 | 系统必须检测禁用特征、明显代理变量、诱导/预设结论和无关敏感问题 | 命中时阻断相关问题并进入质量/合规复核 |
| FR-112 | 每个简报版本必须钉住 Round、受众、评分卡和输入清单；任一变化使旧简报失效 | 旧链接只读；不能编辑或继续发送 |
| FR-113 | 产品内简报任务是权威入口；外部通知只含最小信息、安全深链和唯一动作键 | 通知重试不重复建任务；错误收件人为 P0 |
| FR-114 | 面试官可以接受、编辑、忽略或报告建议问题，反馈与候选人评价隔离 | 反馈不能自动改画像、评分卡或成为负面候选人标签 |

### 8.3 轮次完成件与终面包编译

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-115 | 每个已完成 Round 必须形成当前轮次完成件 | 必含当前已确认评价、当前人工轮次决定、证据集合 hash 和版本 |
| FR-116 | 终面包只接受当前有效轮次完成件，不接受草稿、评论、未确认版或 superseded 决定 | 输入清单在编译前和读取/使用前重验 |
| FR-117 | 轮次豁免必须由有权限的人提交，保存范围、原因、时间、依据和权限快照 | 豁免不可由 Agent 自动提出后直接生效，也不生成能力证据 |
| FR-118 | 相同 EvidencePointer 在多轮/多维度复用时必须去重并显式标记复用 | 覆盖率和证据数量不能因重复引用被放大 |
| FR-119 | 汇总必须按评分卡维度保留支持、反证/差异、上下文限制和信息不足 | 不把跨轮差异静默平均成一个分数 |
| FR-120 | 每条关键汇总 claim 必须回到来源轮次、评价版本和 Evidence Atom | 仅引用面试官印象或无 locator 的结论不得发布 |
| FR-121 | 画像、评分卡、计划和轮次输入版本必须兼容；迁移只能使用有权限的迁移记录 | 新模板发布不能静默改变在途包 |
| FR-122 | 终面包结构不得包含录用/淘汰/推进建议、候选人排名或单一总体人格/能力分 | Prompt、Schema 和发布门三层阻断 |
| FR-123 | 每个包必须有不可变 package_id/version、Input Manifest、内容 hash、编译器/model/prompt/template 版本 | 重编译产生新版本，不覆盖旧包 |
| FR-124 | 同一输入 hash 并发或重复编译只能形成一个当前包并返回原结果 | 同键异载拒绝并报警 |

### 8.4 缺项、跨轮问题与豁免

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-125 | 系统必须区分缺少 Round、确认、决定、证据、维度覆盖和版本有效性 | 缺项不能被“暂无明显风险”等摘要掩盖 |
| FR-126 | Agent 可以提出跨轮问题候选，但每个候选必须引用双方 claim、语境和证据 | Agent 不选择哪一轮正确、不自动改分 |
| FR-127 | 问题严重度和阻断性由批准规则或有权限的人确定 | 未处理 P0/P1 不得标记终面评估就绪 |
| FR-128 | 允许非阻断的问题必须记录分类人/策略、理由、责任人和后续处理 | 终面包中持续显式披露，不能默认折叠隐藏 |
| FR-129 | 豁免必须显示其对证据覆盖和信息不足的影响 | “流程完整”不能冒充“能力证据完整” |
| FR-130 | 关闭问题必须产生解决记录；不得通过删除问题卡或改摘要静默消失 | 输入变化时已解决问题也要按 causal version 重验 |

### 8.5 就绪、分发与失效

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-131 | 只有当前包完整有效、最终招聘决策人权限有效、产品内权威审阅任务创建成功且无阻断项，才能标记 `FINAL_ASSESSMENT_READY` | 不能仅凭“所有 Round 显示完成”标记 |
| FR-132 | `FINAL_ASSESSMENT_READY` 只表示材料就绪，不得自动关闭案件、作最终决定或触发候选人沟通 | 最终决定永久由有权限的人主动提交 |
| FR-133 | 最终审阅任务必须引用唯一当前包和展示版本，且不得预选决定 | 旧页面、旧卡片和旧 package version 不能写当前状态 |
| FR-134 | 外部通知失败必须重试/升级并形成异常；不得靠重复创建权威任务恢复 | 未按策略完成送达/确认时不计入可信托管完成 |
| FR-135 | 任一输入被 supersede、撤销、删除、过期或因计划修订失配时，当前包在继续读取/使用前必须判为失效 | 失效传播延迟不能让旧包继续支持新决定 |
| FR-136 | 包失效后必须撤回或标旧当前审阅任务，Case 从 `FINAL_ASSESSMENT_READY` 回 `INTERVIEWING` | 旧包保留只读审计，不被静默改写 |
| FR-137 | 重复、乱序和迟到的完成/失效/通知事件不得重复建包、建任务或复活旧包 | 使用 aggregate version、input hash、task revision 和 lifecycle epoch |
| FR-138 | 人工纠正通过 supersede 输入、解决问题和重新编译完成，不允许原地编辑已发布终面包 | 新旧依据和差异均可审计 |

### 8.6 审计、隐私与运行

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-139 | 记录计划/轮次/简报/问题/豁免/编译/读取/分发/失效/导出全链路审计 | 可还原某一决策时点可见的包版本和字段范围 |
| FR-140 | 简报、终面包、事件和日志必须按最小必要、分阶段披露、字段权限、保留和删除规则处理 | 删除/撤回传播到派生简报和包副本并有完成回执 |

## 9. 业务规则与不变量

1. ApplicationCase 仍只有六个顶层阶段；多轮进度不新增案件主状态。
2. Interview Plan 是有向无环图；Round 是计划步骤的一次业务实例，重复轮次必须是新实例。
3. 一个 Round 同时只有一个当前轮次完成件；旧评价或决定被 supersede 时，完成件立即失效。
4. 轮次豁免只满足流程要求，不产生证据、分数或“通过”含义。
5. 面前追问简报在独立评价前采用分阶段披露；例外必须有目的、权限、字段和审计。
6. Agent 不得通过“下一轮已激活”反推或展示前轮招聘决定。
7. 终面包只引用当前轮次完成件和有效豁免；草稿、评论和未确认版不进入。
8. 同一证据被多个 claim 引用不增加其事实权重或覆盖分母。
9. 跨轮问题候选不等于事实冲突；最终分类与处置不能由 Agent 单方完成。
10. 信息不足必须显式存在，不能被补写成负面评价或平均分。
11. 终面包是不可变编译物；输入变化产生失效事实和新版本，不原地刷新。
12. `FINAL_ASSESSMENT_READY` 不等于 HIRE、NO_HIRE、终面通过或案件关闭。
13. 最终审阅任务只能引用一个当前有效包；外部通知不是权威状态源。
14. 读取终面包和提交任何最终决定前都必须重验 package validity、task revision、version 和 epoch。
15. 暂停、关闭、撤回、删除和权限撤销优先于待执行简报/终面任务分发。
16. 已删除、过期或用途不再允许的证据不能继续支持新简报或终面包。
17. 事件与指标只承载稳定 ID、版本、hash 和受控摘要，不复制逐字稿或候选人敏感内容。
18. Brief/Package 质量反馈不自动改变画像、评分卡、计划或候选人标签。

## 10. 异常目录

| 代码 | 异常 | 默认处理 |
|---|---|---|
| PLAN_VERSION_MISSING | 案件没有当前面试计划版本 | 招聘运营/用人负责人确认计划 |
| PLAN_CYCLE_DETECTED | 轮次依赖图存在环 | 阻断编排，修订计划版本 |
| ROUND_PREREQUISITE_UNMET | 下一轮前置完成件/豁免不满足 | 等待前置或人工纠正规则 |
| ROUND_COMPLETION_STALE | 完成件引用旧评价/决定/证据 | 失效下游并重建完成件 |
| BRIEF_INPUT_STALE | 简报输入或受众修订已变化 | 撤回旧简报并重编译 |
| BRIEF_POLICY_BLOCKED | 问题命中敏感/诱导/披露策略 | 质量/隐私复核，不静默发布 |
| BRIEF_RECIPIENT_UNAUTHORIZED | 指定面试官权限或分配失效 | 阻断分发，修正受众 |
| REQUIRED_ROUND_INCOMPLETE | 必需轮次无完成件/有效豁免 | 不编译或标记终面就绪 |
| ROUND_WAIVER_INVALID | 豁免无权限、过期或范围不匹配 | 撤销豁免，恢复缺项 |
| PACKAGE_INPUT_VERSION_MISMATCH | 输入版本不兼容或不当前 | 阻断并生成影响清单 |
| PACKAGE_EVIDENCE_GAP | 关键汇总 claim 无证据或必需维度无处置 | 补证、标记信息不足或人工复核 |
| CROSS_ROUND_ISSUE_BLOCKING | 未处理 P0/P1 跨轮问题 | 指定 Owner，终面包不就绪 |
| PACKAGE_DECISION_FIELD_DETECTED | 包含招聘建议/决定/排名字段 | P0 阻断、保留失败产物和根因 |
| PACKAGE_INVALIDATED | 当前包输入被替代/删除/过期 | 撤回任务、Case 回 INTERVIEWING、重编译 |
| FINAL_OWNER_MISSING | 最终招聘决策人无法解析或无权限 | 业务 Owner 修正责任人 |
| FINAL_TASK_DELIVERY_FAILED | 安全通知未送达或回执未知 | 有限重试、对账、升级；不重复建任务 |
| CASE_PAUSED_OR_CLOSED | 案件控制态不允许继续 | 取消未执行动作，保留审计 |
| DELETION_IN_PROGRESS | 权利请求处理中 | 停止生成/分发，等待血缘处置完成 |

## 11. 事件与指标采集

| 事件 | 用途 |
|---|---|
| InterviewPlanPinned / Amended | 计划版本、修订和影响范围 |
| InterviewRoundCompleted / RoundCompletionRecordInvalidated | G1a 输入有效性与下游失效 |
| InterviewRoundActivated / Repeated | 自动续轮与重复轮次 |
| InterviewBriefGenerated / Blocked / Delivered / Invalidated | 简报时效、质量、送达和失效 |
| InterviewBriefFeedbackRecorded | 接受/编辑/忽略/问题类型，不作为候选人标签 |
| RoundWaiverRecorded / Superseded | 豁免率、权限与证据影响 |
| CrossRoundIssueDetected / Classified / Resolved | 问题准确性、阻断和恢复时长 |
| FinalAssessmentCompilationRequested | 编译基线与输入 hash |
| FinalAssessmentPackageReady / Invalidated | 包生成、失效率与恢复 |
| FinalAssessmentReviewTaskCreated / Delivered / Retracted | “摆上桌”与通知可靠性 |
| FinalAssessmentMarkedReady | 有效终面评估就绪率 |
| ExternalActionSuppressed / ExceptionBundleOpened / Resolved | 控制态与人工恢复 |

事件只含 ID、版本、hash、分类和受控摘要；简报问题正文、完整评价和逐字稿保存在受权限控制的内容存储，不复制到分析事件。

## 12. 质量与运行指标

| 维度 | 指标 | G1b 发布原则 |
|---|---|---|
| 安全 | Agent 决定/推荐字段、旧包用于新决定、错误收件人、越权披露 | 必须为 0 |
| 完整性 | 错误标记 `FINAL_ASSESSMENT_READY` | 必须为 0 |
| 证据 | 关键汇总 claim 证据覆盖率 | 必须为 100% |
| 证据 | 重复证据导致的虚假覆盖/计数 | 必须为 0 |
| 偏见 | 独立评价前泄露前轮总分/决定/身份化意见 | 必须为 0 |
| 问题质量 | 禁用/敏感/诱导问题进入已发布简报 | 必须为 0 |
| 可靠性 | 重复当前包、重复权威任务、旧版本复活 | 必须为 0 |
| 采用 | 简报接受/编辑/忽略、终面包有效使用率 | 先分岗位建立基线，不把“接受”当正确 |
| 时效 | 前轮完成到下一轮就绪；最后输入到终面包就绪 P50/P95 | Gate 0 基线后冻结，不拍脑袋承诺 |
| 问题 | 问题候选精确率、漏报严重问题、解决时长 | 按类型/岗位分层；P0/P1 漏报为红线 |
| 失效 | 包失效率、失效发现/撤回/重编译时长 | 必须证明读取/使用前重验 |
| 公平 | 简报问题与汇总错误按岗位/语言/群体分层 | 所有市场发布闸门；样本不足披露 |
| 成本 | 每案编译、存储、通知、人工问题复核成本 | 全成本口径，不只算模型 Token |

## 13. 验收场景

| ID | Given | When | Then |
|---|---|---|---|
| AT-101 | 两个必需轮次，均有当前完成件 | 第二轮完成 | 自动编译当前包、创建权威审阅任务，Case 到 FINAL_ASSESSMENT_READY；没有最终决定 |
| AT-102 | 第一轮评价已确认但无人类轮次决定 | 编排器计算下一步 | 不激活下一轮，明确等待人类决定 |
| AT-103 | 人类决定 REPEAT_ROUND | 控制面执行 | 创建新 Round ID 并关联来源；原轮历史不变 |
| AT-104 | 两个并行前置只完成一个 | 后续 Round 依赖 ALL | 不激活；第二个前置完成后只激活一次 |
| AT-105 | 前轮有总分和 STOP/ADVANCE 决定 | 生成下一轮面前简报 | 简报只显示证据缺口/问题，不泄露总分、决定或身份化意见 |
| AT-106 | 建议问题涉及婚育/健康或预设候选人撒谎 | 发布校验 | 问题被阻断并记录 BRIEF_POLICY_BLOCKED，不静默发布 |
| AT-107 | 简报发布后下一轮面试官改派 | 旧面试官打开链接 | 链接只读/无权，新受众得到唯一当前简报任务 |
| AT-108 | 同一逐字片段被三轮/五个 claim 引用 | 编译覆盖 | EvidencePointer 只计一个事实来源，复用显式展示 |
| AT-109 | 一个必需轮次无完成件或豁免 | 请求编译 | 返回 REQUIRED_ROUND_INCOMPLETE，不生成当前包 |
| AT-110 | 有权限的人豁免一个必需轮次 | 编译终面包 | 流程可完整，但包显著显示豁免、理由和证据缺口，不写“通过” |
| AT-111 | 两轮对同一关键维度形成相反 claim，问题为 P1 未处理 | 请求标记就绪 | 包可保存草稿但 Case 不进入 FINAL_ASSESSMENT_READY |
| AT-112 | 语境差异被人工分类为非阻断 | 编译终面包 | 问题、分类人/规则和理由在包中持续可见 |
| AT-113 | 模型输出 HIRE 建议或候选人排名 | 发布校验 | PACKAGE_DECISION_FIELD_DETECTED，包不发布 |
| AT-114 | Case 已终面就绪，第一轮确认评价被修订 | 新版本生效 | 当前包/任务失效，Case 回 INTERVIEWING，重编译后生成新版本 |
| AT-115 | 最终决策人打开旧包链接 | 输入已失效 | 显示历史只读与当前包入口，旧链接不能支持新决定 |
| AT-116 | 编译事件和通知回调各重复 10 次且乱序 | 控制面处理 | 只有一个当前包、一个权威任务和一个有效业务通知效果 |
| AT-117 | 删除请求覆盖某轮证据与派生包 | 删除执行 | 停止分发、包失效、按血缘处置并形成回执 |
| AT-118 | 产品内任务创建成功但外部通知持续失败 | 重试耗尽 | Case 可保持材料就绪但运行态为 NEEDS_HUMAN/异常；不计入可信托管完成，不重复建任务 |

## 14. 发布、放权与回滚

### 14.1 发布阶梯

| 阶段 | 行为 | 放行要求 |
|---|---|---|
| 合成回放 | 用合成 Round Completion Record 验证依赖、缺项、冲突、豁免、失效 | 全部 AT 可重复、无决定字段、旧包不能复活 |
| 离线历史回放 | 对经批准历史案件生成简报/终面包，不展示给业务用户 | 金标、字段披露、问题错误分类、成本和公平分层可用 |
| A0 影子 | 对真实新完成轮次生成，但不向下一轮面试官/最终决策人展示 | 连续样本无 P0，输入/失效/权限门通过 |
| A1 建议 | 小范围产品内展示；Brief/Package 分发需人确认 | 零错误收件人、零旧包使用、可暂停/撤回/接管 |
| A2 规则内自动 | 人类轮次决定后自动激活下一轮、创建合规简报/最终任务并规则内提醒 | 达到冻结的质量、时效、问题恢复和可靠性门槛 |

招聘决定永久不开放给 Agent。G1b 即使达到 A2，也只自动准备和分发材料，不自动提交最终决定。

### 14.2 立即回滚条件

- 独立评价前泄露前轮决定、总分或身份化意见。
- 禁用/敏感/诱导问题进入已发布简报。
- Agent 生成招聘决定、排名或单一总体人格/能力分。
- 缺必需输入、未处理 P0/P1 或无权限豁免却标记终面就绪。
- 已失效包继续可用于新的最终决定。
- 错误收件人、跨案件/跨租户证据、重复权威任务或无法停止外发。
- 删除/撤回/超保留期后仍继续生成或分发。

回滚时先降级受影响动作：撤回当前 Brief/Task、将 Case 回到可解释阶段、冻结外发、保留审计、人工接管、离线重放并重新过门。

## 15. Gate 0 前必须拍板

| 决策 | 推荐默认 | 决策人 |
|---|---|---|
| 首批面试计划 | 1 个主岗位、2–4 个稳定轮次，先不做复杂条件分支 | 用人负责人 + 招聘运营 |
| 依赖语义 | 支持顺序与少量 ALL 并行依赖；不开放任意表达式 | 产品 + 技术 |
| 重复轮次 | 新 Round ID，关联原轮和人类理由 | 招聘运营 |
| 豁免权限 | 仅指定用人负责人/招聘负责人；按轮次类型配置 | 用人负责人 + 隐私法务 |
| 分阶段披露 | 独立评价前只看缺口/问题，不看前轮总分/决定 | 用人负责人 + 隐私/公平 Owner |
| 问题严重度 | P0/P1 阻断；非阻断必须有人/策略负责并显式展示 | QA + 招聘运营 + 隐私法务 |
| 最终权威入口 | 产品内任务；选择一个主 IM/邮件作通知 | 产品 + 技术 |
| 最终决策人解析 | 每岗位需求一个当前 Owner + 代理/离职恢复规则 | Sponsor + 招聘运营 |
| G1b 数据范围 | 只用当前已确认评价和允许使用的证据；不训练通用模型 | 隐私法务 + AI/Data |
| 质量门槛 | 用历史/合成基线冻结，安全红线为 0 | Product + QA + AI/Data |

## 16. Definition of Done

只有以下全部有可检查证据，G1b 才算完成：

- FR-101 至 FR-140 有实现、测试和责任人。
- AT-101 至 AT-118 自动化或可重复人工验收通过。
- 至少覆盖顺序轮次、并行前置、重复轮次、豁免、阻断问题、非阻断问题、输入失效和通知失败。
- 合法真实样本完成离线、A0 和 A1；问题/简报/终面包按岗位、语言和群体分层评估。
- 决定字段、前轮决定泄露、错误收件人、错误终面就绪、旧包使用和重复权威任务均为 0。
- 暂停、改派、权限撤销、证据修订、删除、失效、撤回任务和重编译完成真实演练。
- 每个终面包能还原 Input Manifest、编译版本、问题/豁免和当时可见字段。
- 用人负责人、招聘运营、技术、AI/数据、QA、隐私法务和试点最终决策人完成签字放行。

~~~yaml
acceptance:
  asserts:
    - "scope_stops_at_final_assessment_ready_not_final_decision"
    - "only_current_round_completion_records_feed_the_package"
    - "next_round_activation_requires_a_valid_human_round_decision"
    - "interview_brief_uses_staged_disclosure"
    - "waiver_satisfies_workflow_not_evidence"
    - "cross_round_issues_are_visible_and_not_agent_resolved"
    - "every_critical_package_claim_has_source_evidence"
    - "package_contains_no_hiring_recommendation_or_ranking"
    - "input_change_invalidates_package_and_current_task"
    - "final_assessment_ready_does_not_close_the_case"
    - "duplicate_and_out_of_order_events_are_idempotent"
    - "pause_withdrawal_and_deletion_stop_pending_distribution"
~~~
