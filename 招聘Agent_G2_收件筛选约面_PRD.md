# 招聘 Agent G2 PRD：从简历收件到可信面试交接

版本：v0.2  
日期：2026-08-10  
状态：Gate 0 评审稿  
产品边界：白名单来源产生简历投递 → 唯一路由并开案 → 证据化匹配材料 → 部门人工决定 → 候选人协调与面试预定 → 采集闸门 → G1 面试证据交接  
发布结论：真实数据、真实 A0/A1、外部发送/写入、录制和工程发布仍为 No-go

相关材料：

- [产品落地总方案](./招聘Agent产品落地总方案.md)
- [领域语言](./CONTEXT.md)
- [领域与事件规格](./招聘Agent_领域与事件规格.md)
- [G1a MVP PRD](./招聘Agent_G1_MVP_PRD.md)
- [G1b 多轮与终面评估包 PRD](./招聘Agent_G1b_终面评估包_PRD.md)
- [ADR-0007：时段提案不等于面试预定](./docs/adr/0007-scheduling-proposal-is-not-a-booking.md)
- [ADR-0008：录制告知不等于采集授权](./docs/adr/0008-recording-notice-is-not-consent.md)
- [Gate 0 执行包](./招聘Agent_Gate0执行包.md)

## 1. 要解决的问题

G1a/G1b 已经定义面试发生后的证据闭环、多轮串联和终面评估包，但从简历进入组织到一次面试能够合法、准确地发生，仍有六个依赖 HR 搬运的断点：

1. 邮箱、ATS、内推和转发产生重复或模糊投递，容易重复开案、跨岗位串档或漏处理。
2. 简历解析与人岗匹配常被压缩成一个分数，来源证据、信息不足、禁用字段和版本变化不可解释。
3. 用人部门通过聊天消息或口头反馈决定，Agent 无法区分沉默、旧卡片和有效的人类决定。
4. 候选人选中时段后，日历冲突、部分成功、响应丢失和迟到回调可能造成双重邀请或旧预约复活。
5. 会议邀请中的录制条款容易被误当作同意，晚加入、拒绝、撤回和无录制路线没有进入运行闸门。
6. 邮件、日历和会议失败时，系统可能无限重试、静默停滞或要求 HR 逐案救火，而不是交付一个可决策异常包。

本期要证明：

> 一个批准来源的新投递可以自动启动并持续推进到“有权限的人关闭案件”或“当前面试会话向 G1 形成可信交接”，正常链无需 HR 手动点火或搬状态；路由冲突、部门招聘决定、候选人选时和参与人采集选择仍由相应的人完成，Agent 不越权。

## 2. 成功定义

G2 成功必须同时满足：

- 合格来源事件能自动创建或附加简历投递，重复消息、附件和回调不产生重复业务效果。
- 只有候选人、岗位需求和招聘周期唯一解析后才能开案；同人多岗、多周期严格隔离，已开案路由纠正不改写 ApplicationKey 或搬运历史。
- 人岗匹配材料逐维呈现支持、反证、未知和原文证据；低分、硬条件或模型输出都不能自动淘汰。
- 部门决策任务在产品内唯一且版本化；进入 AWAITING_DEPARTMENT_DECISION 时任务必须原子存在，INVITE、HOLD、REJECT 只能由有权限的人主动提交。
- 候选人选择时段后仍需重验忙闲；只有当前预约修订的日历、会议和邀请写入回执齐全才提交面试预定。
- 邀请告知、参与人采集选择、开始/停止请求和平台已开始/已停止彼此分离；无有效凭证时只能走无录制路线或阻断采集，不能剥夺面试机会。
- 暂停、撤回、删除、关闭和权限撤销优先于队列与迟到回调；安全停止和补偿动作仍可执行。
- 每个外部效果都能回到当前案件、当前目标修订、动作策略、执行尝试和连接器回执。
- 有限重试耗尽后只形成一个当前异常包，包含事实、尝试、风险、选项、Owner 和恢复条件。
- 录制或无录制路线都只向 G1 交付当前、完整、可追溯的不可变输入；输入失效在 G1 读取/使用前也必须被阻断，不伪造录音、逐字稿或证据质量。

北极星子指标：**G2 可信托管完成率**。  
定义：在目标 SLO 内从合格简历投递到“有权限的人关闭案件”或“当前面试会话向 G1 交接就绪”，期间无计划外 HR 介入、无错误业务效果且无安全/合规事件的投递数 ÷ 所有进入 G2 托管范围的合格投递数。

效率、采用、时效和自动化率在 Gate 0 基线形成前不拍具体数字；安全红线始终以事件数为 0 衡量。

## 3. 范围

### 3.1 本期包含

- 一个内部组织、1–2 个试点岗位和一个明确招聘周期。
- 一个主简历来源、一个部门协作入口、一个日历/会议生态；人工入口只作为显式降级。
- 来源、消息、附件和内容级去重；原件、结构化简历版本及字段来源定位。
- 候选人、岗位需求和招聘周期的唯一路由、人工复核与同一案件附加新来源。
- 画像版本锁定、筛选输入清单、证据化人岗匹配材料和发布校验。
- 部门决定请求、产品内权威任务、安全通知、有限催办、升级和 HOLD 回访时间。
- 部门决定 INVITE 后首轮激活；后续轮次只消费 G1b 的 InterviewRoundActivated 事实。
- 候选人协调请求、时区、可访问性需求、时段提案、选时、有限改期和人工协助入口。
- 日历忙闲读取、日历/会议/邀请写入、预约修订、对账、补偿取消与回执。
- 录制告知、参与人级采集选择凭证、采集闸门、无录制路线、晚加入和会中撤回。
- 面试结束后向 G1 交付当前案件、轮次、会话、预约修订、参与人、采集模式、互斥证据路线、凭证与资产/笔记引用，并支持失效与替代。
- 动作级 A0–A3、暂停、熔断、幂等、有限重试、异常包、审计和删除传播。

### 3.2 本期不包含

- 主动寻访、自动 shortlist、跨候选人自动排名或人才库运营。
- Agent 自动提交进入面试、HOLD、淘汰、拒信、轮次决定、录用或最终淘汰。
- G1a 的转写、证据评价、面试官确认和轮次归档实现。
- G1b 的轮次依赖计算、面前追问简报、跨轮问题和终面评估包实现。
- Offer、背调、薪酬、最终决定后沟通和入职。
- 画像自动学习、自动调权重或把历史通过结果直接作为训练标签。
- 多租户、多 ATS、多会议平台、任意工作流表达式或跨组织调度。
- 在当前 No-go 未解除时使用真实个人信息、真实账号、真实录制、真实外发或真实 A0/A1。

### 3.3 合格输入与出口

合格入口同时满足：

1. 来源在批准白名单内，来源身份与权限可证明。
2. 附件类型、大小、完整性、恶意内容和加密状态通过接收门。
3. 当前数据地图、处理目的、保留和访问策略允许接收该类材料。
4. 连接器至少支持本阶段所需的可对账读取；当前 Gate 仍只允许合成或匿名化输入。

G2 的两个合法出口：

- 有权限的人作出 REJECT、候选人撤回、岗位需求关闭或其他合法关闭事实，ApplicationCase 进入 CLOSED。
- 当前面试会话结束且采集尝试、片段处置和最终对账已结算，形成 RECORDED_EVIDENCE 或 AUTHORIZED_NOTES_ONLY 的面试证据交接，进入 G1a 证据处理；后者表示本次交接不引用音视频证据，但仍诚实保留实际采集历史。

## 4. 用户与权限

| 角色 | 可以做 | 不能做 |
|---|---|---|
| 候选人 | 查看自己的协调请求；选择/改期；声明不可用或可访问性需求；作采集选择并撤回 | 查看内部匹配、他人日历正文、内部评价或其他候选人 |
| 用人部门负责人 | 查看当前匹配材料和证据；提交 INVITE/HOLD/REJECT；处理部门异常 | 让 Agent 代选；使用旧卡片覆盖当前决定；查看禁用字段 |
| 指定面试官 | 授权忙闲范围；确认自己的参与；作自己的采集选择 | 暴露日历正文；继承或代替其他参与人选择 |
| 招聘 HR | 监督案件、SLA 和异常；处理路由、Owner 和复杂协调；暂停/接管/恢复 | 逐案手动点火正常链；绕过同意闸门；无痕修改决定 |
| 招聘运营管理员 | 配置来源、画像、SLA、模板、提醒、调度约束和动作等级 | 静默改变在途案件钉住版本；扩大敏感字段用途 |
| 隐私/安全管理员 | 管理告知、采集选择、撤回、删除、字段权限、供应商和审计 | 修改招聘决定或候选人业务评价 |
| 质量标注员 | 在批准的隔离任务中标注解析、证据和公平错误 | 搜索或导出全部候选人材料；作招聘决定 |
| Agent 智能引擎 | 解析非结构化材料；生成匹配草稿；提出时段和动作建议 | 修改状态；直接调用外部写工具；作招聘决定；绕过采集闸门 |
| 确定性控制面 | 按权限、版本、策略和幂等规则执行迁移与外部动作 | 伪造人的决定；把模型分数转成招聘结果 |

权限同时受租户、部门、岗位需求、招聘周期、案件、字段、处理目的、动作类型、披露阶段和时间范围约束。

## 5. 产品组成

| 产品模块 | 核心产物 | 价值 |
|---|---|---|
| 投递收件箱 | ResumeSubmission、原件引用、结构化简历版本、路由候选 | 不漏件、不重件，不把模糊投递强行开案 |
| 路由与开案台 | 当前申请路由、ApplicationKey、复核任务、来源附加记录 | 同人多岗/多周期隔离，形成唯一案件主线 |
| 筛选工作台 | Screening Input Manifest、人岗匹配材料、证据定位、发布门 | 让用人部门看证据而非只看模型分 |
| 部门决定中心 | 当前决定请求、安全卡片、HOLD 回访、有限催办 | 人作决定，Agent 负责持续推动和超时升级 |
| 候选人协调页 | 当前协调请求、时段提案、选择/改期、时区和帮助入口 | 候选人自助完成必要输入，减少 HR 往返 |
| 预约协调器 | Appointment Revision、Booking Receipt Set、对账/补偿 | 防止部分成功、重复建会和旧预约复活 |
| 告知与采集闸门 | Recording Notice、Consent Receipt、Capture Gate、无录制路线 | 把合法选择变成运行事实，而不是一段邀请文案 |
| G1 交接器 | Interview Evidence Handoff | 把合法发生的会话交给证据引擎，不伪造缺失材料 |
| Agent 控制塔 | RuntimeEnvelope、ActionExecution、ExceptionBundle、审计 | 正常链安静推进，异常交付可决策单元 |

## 6. 端到端主流程

| 步骤 | 触发与系统动作 | 权威产物 | 状态/权限要求 |
|---|---|---|---|
| 1 | 白名单来源产生新消息或资源事件 | ResumeSubmission RECEIVED | 来源、附件和内容幂等；材料只作为不可信数据 |
| 2 | 文件门通过后解析并保存字段定位 | Structured Resume Version | 新版本不覆盖历史；失败进入有限异常 |
| 3 | 解析候选人、岗位需求和招聘周期 | Routing Resolution 或 Review Required | 只有唯一当前路由可开案；纠正生成新 revision |
| 4 | 打开或附加到 ApplicationCase | ApplicationKey 与当前来源关系 | ApplicationKey 不可变；改键先 supersede 旧关系/动作并明确旧案处置；同人多岗隔离 |
| 5 | 钉住简历、画像、允许字段和策略 | Screening Input Manifest | Case RECEIVED→SCREENING |
| 6 | 生成并发布逐维匹配材料 | READY Match Assessment | 关键 claim 有证据；无决定字段；低分不淘汰；不单独迁移 Case |
| 7 | 原子钉住当前材料、创建唯一请求并安全通知 | Current Department Decision Request | 同一 ApplicationCase 事务内 Case→AWAITING_DEPARTMENT_DECISION，不出现有阶段无任务 |
| 8 | 有权限的人提交 INVITE/HOLD/REJECT | Human Decision Record + Request Revision | 沉默不是决定；HOLD→ON_HOLD 并停催办；旧任务不能写当前状态 |
| 9 | INVITE 后钉住计划并激活首轮 | InterviewRound READY_TO_SCHEDULE | Case→INTERVIEWING；后续轮只消费 G1b 激活事实 |
| 10 | 加载授权忙闲与批准约束 | Candidate Coordination Request | 不读取或披露日历正文 |
| 11 | 生成带版本和有效期的时段提案 | Scheduling Proposal | 提案和候选人选择都不是预定 |
| 12 | 候选人选择后重验忙闲并提出预约修订 | Pending Appointment Revision | 旧/过期选择拒绝；并发冲突回到新提案 |
| 13 | 控制面执行日历、会议和邀请写入并对账 | Interview Booking 或异常 | 回执齐全才提交当前预定；部分成功不显示已排期 |
| 14 | 候选人改期 | 新 Appointment Revision | 新预定提交前旧预定仍有效；成功后补偿旧资源 |
| 15 | 送达录制告知并记录参与人逐目的选择 | Notice/Consent Receipts | 接受邀请不等于同意；无录制路线等价可用 |
| 16 | 面试开始时记录当前参与人 | InterviewSession IN_PROGRESS | StartInterview 只写 Session，不隐式请求采集；Round 由独立命令推进 |
| 17 | 每次开始/继续采集和参与人变化时重验 | Capture Gate Evaluation | 所有当前在场且媒体可能进入采集的参与人都过门；未通过则无录制或请求停止 |
| 18 | 控制面请求采集并等待平台确认 | STARTING/ON 或 STOPPING/STOPPED | Request 与 Confirm 分离；未确认停止不声称 STOPPED，状态不明按 P0 |
| 19 | 面试结束、采集结算并形成不可变证据交接 | Current Interview Evidence Handoff | RECORDED_EVIDENCE/AUTHORIZED_NOTES_ONLY 互斥，输入完整、采集历史诚实且可失效 |
| 20 | G1 接收同一交接键并在使用前重验 | G1a Input Accepted | 只形成一个当前输入；Invalidated/迟到 Accepted 不复活，G2 不伪造转写或评价 |

## 7. 产品界面与任务

### 7.1 投递与路由队列

必须显示：

- 来源、接收时间、附件分类、内容指纹、解析质量和当前处理目的。
- 当前结构化简历版本及字段来源定位；不默认展示被隔离的敏感字段。
- 路由到候选人、岗位需求和招聘周期的候选项、冲突原因与恢复动作。
- 重复来源附加到既有案件的审计关系，不将其显示成第二个候选人流程。
- 当前 Owner、SLA、有限重试、暂停与异常包。

不得提供“忽略冲突直接开案”或“按邮箱/手机号直接合并身份”的快捷键。

### 7.2 人岗匹配与部门决定

匹配材料按画像维度展示：

- 必要条件、支持证据、反证、未知、不适用和原文定位。
- 结构化简历、画像、策略和生成版本；当前性与失效原因。
- 禁用字段/代理变量/提示注入污染的阻断结果。
- result band 只作材料分类，不能变成跨候选人排名或招聘建议。

部门卡片必须显示当前材料版本、证据、信息不足、决定选项、截止与后果；产品内任务是权威入口，外部消息只提供最小信息和安全深链。INVITE/HOLD/REJECT 不预选，提交时记录人、权限、依据和版本。

### 7.3 候选人协调页

- 移动优先，使用候选人时区并同时说明岗位、轮次、预计时长和参与人角色。
- 展示当前提案的可选时段、有效期和时区；过期或并发失效时清楚刷新。
- 支持选择、改选、改期、全部不可用、请求人工协助和可访问性需求。
- 使用限时、限案件、限用途、限修订的安全入口；错人、重放和旧链接不得改变状态。
- 录制告知与采集选择显著、逐目的、可撤回；拒绝后直接进入同等可用的无录制路线。

### 7.4 面试预约与对账

运营视图必须区分：

- 候选人已选时段。
- 待提交预约修订。
- 当前有效面试预定。
- 日历、会议、邀请各自写入/对账结果。
- 改期中的旧有效预定与新待提交修订。
- 外部重复资源、迟到回调和需要补偿的资源。

单一连接器成功不能把整个会话或轮次显示为已排期；一轮有多个必需会话时，只有全部必需会话当前有效预定，轮次才投影为 SCHEDULED。

### 7.5 采集闸门

必须展示：

- 当前会话参与人及在场状态、告知版本、处理目的和各自当前选择。
- 当前采集模式：RECORDING 或 NO_RECORDING；证据交接路线另列 RECORDED_EVIDENCE 或 AUTHORIZED_NOTES_ONLY。
- 闸门最近计算时间、适用 ProcessingControl set hash/引用、允许/阻断原因，以及采集请求、平台观察和业务确认状态。
- 晚加入、转发邀请、撤回、限制处理和状态不明时的立即动作。
- 无录制路线对面试与评价机会等价，且相关选择与原因不进入匹配或评价。

### 7.6 异常包

每个异常包至少包含：标题、案件/投递/会话范围、当前事实、Agent 已尝试动作、外部回执、风险、可选动作、建议、Owner、截止、恢复凭证和是否阻断可信托管完成。错误码只作为审计字段，不能替代可决策说明。

## 8. 功能需求

### 8.1 收件与来源

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-201 | 只接入经批准的投递来源，并记录来源系统、消息/资源修订、接收时间和适用处理目的 | 非白名单来源不得进入解析；来源身份与权限可审计 |
| FR-202 | 以租户、来源消息/资源、附件和内容指纹形成收件幂等与关联键 | 重复到达只增加来源审计，不产生重复业务效果 |
| FR-203 | 附件类型、大小、完整性、恶意内容和加密状态必须在解析前检查 | 失败停在投递层，不执行材料内指令或外部链接 |
| FR-204 | 原件和结构化简历都必须版本化，重解析、人工纠正或新来源不得覆盖历史 | 每个字段能回到来源 locator、版本和修订原因 |
| FR-205 | 一封消息内多个附件必须逐件分类和归属，不得把不同候选人材料合并 | 无法唯一拆分时进入复核，不创建案件 |
| FR-206 | 转发、重复附件和 ATS 重复回调只附加来源引用，不重复创建投递效果或案件 | 同键同载返回原结果；同键异载拒绝并报警 |
| FR-207 | 简历正文、附件、链接和元数据中的提示或指令一律视为不可信内容 | 不触发工具、状态、权限、外发或额外数据读取 |
| FR-208 | 解析失败、低质量或非申请判断必须有规则/人工依据、Owner、SLA、审计和恢复入口 | 不静默丢弃、不编造字段、不无限重试 |

### 8.2 解析、身份与唯一路由

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-209 | 每个结构化字段保存来源 locator、解析器/人工修订版本和置信信号 | 低置信字段显式呈现，不伪装成确定事实 |
| FR-210 | 保护字段、敏感字段及明显代理变量必须与匹配输入和普通业务界面隔离 | 使用例外需目的、权限、字段范围、期限和审计 |
| FR-211 | 邮箱、手机号、姓名和简历内容只能作为身份解析信号，不能作为申请案件主键 | 共享邮箱、复用手机号不自动合并 |
| FR-212 | 身份合并/拆分必须有权限、差异、依据和审计；冲突不得由模型自动处理 | 纠正不覆盖原历史，受影响动作与材料重验 |
| FR-213 | 岗位需求和招聘周期必须解析到当前允许处理的唯一对象 | 关闭、越权或未知岗位不得开案 |
| FR-214 | 自动路由只在唯一候选项达到批准门且没有冲突信号时生效 | 置信分本身不等于路由事实 |
| FR-215 | 候选人、岗位需求或招聘周期任一不唯一时进入 ROUTING_REVIEW_REQUIRED | 不得创建缺少 ApplicationKey 的“半个案件” |
| FR-216 | 开案要求完整 ApplicationKey、唯一性、expected version、lifecycle epoch 和幂等键 | 路由不是 ROUTED 时命令拒绝 |
| FR-217 | 同一 ApplicationKey 的新投递附加到原案件；若授权路由纠正改变 ApplicationKey，旧键不得改写，必须先 supersede 旧投递关系及其材料/任务/待执行动作，再按新路由开案或附加 | 旧决定、会议和审计历史不搬运；已有外部效果或其他有效来源时先由人明确补偿/关闭范围 |
| FR-218 | 同一候选人的多岗位、多周期案件必须隔离，任何跨案引用都需业务目的和权限 | 画像、决定、日历、评价和外发不得串案 |

### 8.3 匹配与筛选

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-219 | 开始筛选前必须有可读的当前结构化简历版本和已批准岗位画像版本 | 输入缺失时保持可解释等待/异常，不生成空分数 |
| FR-220 | Screening Input Manifest 钉住简历、画像、允许字段、策略和生成器版本 | 同一输入可重放；输入变化生成新清单 |
| FR-221 | 每个画像标准必须区分支持、反证、未知和不适用 | 信息不足不得被计为负面证据 |
| FR-222 | 必要条件和低匹配结果都只能形成供人审阅的材料，不能直接淘汰或关闭案件 | 无任何 score→REJECT 状态迁移 |
| FR-223 | 每条关键匹配 claim 必须定位到简历原文或批准使用的证据 | 无 locator 或证据相反时发布门阻断 |
| FR-224 | 模型不得用常识补齐候选人没有提供的经历、年限、身份或能力 | 推断与事实分开；未知保持未知 |
| FR-225 | 禁用特征、代理变量或提示注入污染必须阻断相关结论并进入质量/公平复核 | 学校、姓名、口音、空窗等代理风险有测试 |
| FR-226 | 发布门必须校验必需维度、证据覆盖、输入版本、字段策略和禁止决定/排名字段 | 校验失败不创建部门决定请求 |
| FR-227 | Match Assessment 发布后不可原地修改；重跑产生新版本并使旧任务失效 | Case 只有一个当前材料指针 |
| FR-228 | result band 只作材料组织，不能成为招聘决定、跨候选人排名或候选人外发标签 | 权限、Schema 和发布门共同阻断 |

### 8.4 部门决定与催办

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-229 | 只有 READY Match Assessment 可在同一 ApplicationCase 事务内被钉住、创建唯一权威部门决定请求并迁移到 AWAITING_DEPARTMENT_DECISION | Case 不得出现“待部门决定但没有当前 OPEN/ON_HOLD 请求”；通知失败不造第二任务 |
| FR-230 | 决策卡展示证据、反证、未知、版本和时限，不展示保护字段或单一人格/能力分 | 每条关键结论可回源 |
| FR-231 | 外部通知只包含最小信息、安全深链、当前受众和唯一动作键 | 卡片/邮件不是第二个权威任务 |
| FR-232 | INVITE、HOLD、REJECT 必须由当前有权限的人主动提交，Agent 或 Service 身份不得代交 | 不预选、不把沉默/超时解释成决定 |
| FR-233 | INVITE 使 Case 进入 INTERVIEWING；REJECT 人工关闭；候选人拒信是决定后的独立外发动作 | 决定与沟通分别授权、分别留回执 |
| FR-234 | HOLD 保持 Case=AWAITING_DEPARTMENT_DECISION，并把同一 request_id 写成新的 ON_HOLD revision；必须记录 HUMAN Decision Record、原因、revisit_at 和新 SLA generation | HOLD 停止催办；到期仅在材料/Owner/权限仍当前时生成新的 OPEN revision，不替人决定、不复活旧卡片 |
| FR-235 | 当前材料失效必须原子撤销 Case 指针与决定请求并回 SCREENING；旧卡片、旧材料、旧 request revision、权限失效或已处理提交不得改变当前状态 | 返回明确陈旧/已处理结果，不覆盖当前决定；INVITE/REJECT 生效时请求 CLOSED |
| FR-236 | 催办遵守静默时段、最大次数、ordinal、升级链、当前 OPEN 请求/Owner/generation 和幂等；耗尽后开异常包 | 人已回复、ON_HOLD/CLOSED/SUPERSEDED、Owner 改派或阶段变化后不再发送 |

### 8.5 候选人协调与面试预定

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-237 | 只有当前已激活且 READY_TO_SCHEDULE 的必需会话/轮次可以打开协调请求 | 未有人类 INVITE 或 G1b 激活事实时阻断 |
| FR-238 | 日历只读取授权范围内的忙闲、时区和资源可用性，不读取或披露事件正文 | 授权撤销立即使旧忙闲快照不可继续使用 |
| FR-239 | Scheduling Proposal 必须包含当前约束、时区、忙闲快照引用、版本和有效期 | 提案过期或输入变化时旧选择无效 |
| FR-240 | 候选人协调入口使用限时、限案件、限用途和限请求修订的凭证 | 错人、重放、旧 revision 不得查看或修改当前请求 |
| FR-241 | 候选人选择必须携带当前 proposal/version 和唯一 selection action | 同一提案内合法改选产生新选择修订，不覆盖历史 |
| FR-242 | 提出预约修订前必须重新读取当前参与人、资源、忙闲、案件控制态和策略 | 已冲突时不执行外部写，生成新提案或异常 |
| FR-243 | 时段提案与候选人选择都不等于预定；日历、会议和邀请写入回执齐全后才能提交 Interview Booking | 部分成功保持待对账/重试/异常，不显示 SCHEDULED |
| FR-244 | 创建、修改、邀请和取消全部通过 ActionExecution、业务幂等键和执行前重验 | 外部成功但响应丢失时先对账，不重复效果 |
| FR-245 | 邀请显示可信发送主体、时间/时区、会议入口、改期入口和录制告知版本 | 通知送达/已读与资源写入回执分开记录 |
| FR-246 | 改期创建新 Appointment Revision；新预定提交前旧预定仍有效，成功后补偿旧资源 | 旧回调只审计/补偿，不能复活旧修订 |
| FR-247 | 撤回、暂停、关闭或权限撤销时取消未执行生产动作，并对已建外部资源执行允许的安全补偿 | CANCEL_MEETING、REVOKE_LINK 等补偿不被 CLOSED/PAUSED 错误阻断 |
| FR-248 | 改期次数、时间窗、提案轮数和替换面试官范围必须有限 | 无共同时间、规则外调整或耗尽形成异常包 |

### 8.6 录制告知、选择与采集交接

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-249 | 录制告知必须覆盖目的、方式、数据类型、接收方、保留/权利入口和无录制路线，并版本化 | 送达渠道和版本可证明；文案不得默认授权 |
| FR-250 | 采集选择按参与人、会话、目的和告知版本显式取得；接受邀请不等于同意 | 他人选择、旧会话或旧目的凭证不可复用 |
| FR-251 | Consent Receipt 保存主体、选择、时间、渠道、告知版本、目的和撤回状态 | 重复同载返回原结果，同键异载拒绝 |
| FR-252 | 拒绝录制进入等价 capture_mode=NO_RECORDING；面试结束后可在批准笔记完备时形成 AUTHORIZED_NOTES_ONLY 证据路线，不得影响面试机会或进入匹配/评价信号 | 页面和下游数据均不将拒绝解释为负面；采集模式与证据路线不混为一个状态 |
| FR-253 | 只有全部当前在场且其媒体可能进入平台采集的参与人通过 Capture Gate 时才允许 RECORDING | 不能靠漏标“被采集人”绕过晚加入者；未满足时面试仍可开始但采集关闭 |
| FR-254 | StartInterview、EvaluateCaptureGate、RequestCaptureStart 和 ConfirmCaptureStarted 必须分离；每次开始/继续采集前重验案件、预约、参与人、目的、凭证、适用 ProcessingControl set 和平台能力 | 只有匹配当前 session/attempt/gate/action/resource revision 的 Provider 回执能写 ON；旧快照或状态不明时拒绝 |
| FR-255 | 转发邀请、临时观察员和晚加入者不继承他人选择 | 新参与人未完成选择时暂停采集或保持无录制 |
| FR-256 | Consent Receipt 撤回或适用范围内的 ProcessingControl 限制/删除生效后，立即禁止新的开始/继续处理，发出 SAFETY_COMPENSATION 停止请求并隔离 effective_at 后材料 | 只有当前停止回执/可信对账才能标 STOPPED；限制只作用于其主体、案件/会话、目的、操作和数据范围，并按血缘处置 |
| FR-257 | 采集请求、平台观察和业务确认必须分开记录并持续对账 | 意外录制、停止 SLO 内无法确认或状态不明保持 STOPPING/BLOCKED，按 P0 熔断；不得乐观标记已停 |
| FR-258 | 只有 InterviewSession=ENDED 且采集尝试、片段处置与最终对账结算后，才生成一个 current_handoff_ref 与不可变 HandoffInputManifest；STARTING/ON/STOPPING/状态未知一律拒绝。RECORDED_EVIDENCE 必须钉住当前 STOPPED 状态、最终资产 id/version/checksum 和 Consent/Control/Capture 对账引用；AUTHORIZED_NOTES_ONLY 必须钉住笔记 id/version/hash、作者/授权、证据局限、真实 capture history 与 final reconciliation，且不引用音视频作为证据 | 两条路线互斥；“仅笔记证据”不等于“从未采集”，部分采集/撤回场景须先完成停止、片段隔离/删除/处置和对账，不得抹去历史或伪造证据质量 |

### 8.7 控制面、隐私与 G1 交接

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-259 | 模型不能直接写状态或调用外部工具，所有业务效果由确定性命令和 ActionExecution 完成 | Prompt/Resume 注入的 tool execution count 必须为 0 |
| FR-260 | A0–A3 按动作授权；招聘决定永久人审，当前 No-go 下所有真实外部动作禁用 | 内部提醒放权不自动扩散到候选人外发或录制 |
| FR-261 | 暂停、撤回、删除、关闭和权限撤销优先于队列与迟到回调 | 每次执行钉住并重验 lifecycle epoch、processing control set hash/control refs、current routing revision、target revision、policy 和 payload hash |
| FR-262 | 重试、频控、预算、静默时段和对账必须有限，耗尽形成唯一当前 Exception Bundle | 不无限循环、不静默停滞、不重复升级 |
| FR-263 | 全链路执行租户、部门、岗位、案件、字段、主体、目的、操作、数据范围和时间权限，记录访问/决定/外发/补偿/删除审计 | ProcessingControl 不自动扩大到其他案件/主体/用途；事件和指标不复制简历正文、联系方式或完整日历/逐字稿 |
| FR-264 | G1 交接按 case/round/session/appointment/handoff revision 幂等，只形成一个当前输入；任一 causal input 被替代、删除、超保留期或用途/控制不再允许时，当前或已接受 Handoff 在 G1 读取/使用前失效，替代输入只建新 revision | 每个命令只写一个聚合和 Outbox；G2 不重新判断 G1b 轮次依赖、不直接写页面状态，迟到 Accepted 不复活旧输入 |

## 9. 稳定语义不变量

以下 ID 是产品与领域规格共同维护的稳定语义锚点，文案可以演进，含义不能静默改变：

| ID | 不变量 |
|---|---|
| INV-G2-ROUTED-ONLY | ResumeSubmission 只有当前路由为 ROUTED 且 ApplicationKey 完整时才能打开或附加申请案件 |
| INV-G2-ROUTING-CORRECTION | ApplicationKey 不可因路由纠正原地改写；一个投递只有一个当前案件关系，旧关系/依赖动作先失效且旧案处置范围明确后才能落到正确案件 |
| INV-G2-NO-AUTO-REJECT | 低分、必要条件缺失、解析结果和模型输出都不能直接淘汰、关闭或发拒信 |
| INV-G2-HUMAN-DECISION | INVITE、HOLD、REJECT、轮次与最终招聘决定只能由有权限的人提交 |
| INV-G2-PROPOSAL-NOT-BOOKING | 时段提案和候选人选择不是面试预定，预定必须提交当前修订和外部写入回执 |
| INV-G2-CURRENT-REVISION | 旧 request、proposal、appointment、task、card、link 和 callback 不能写当前状态 |
| INV-G2-NOTICE-NOT-CONSENT | 录制告知、接受邀请和入会都不产生采集选择凭证 |
| INV-G2-NO-RECORDING-EQUIVALENCE | 拒绝录制必须有等价无录制路线，且不得成为招聘评价信号 |
| INV-G2-LATE-JOINER-GATE | 晚加入或转发邀请产生的新参与人未通过闸门前不得被采集 |
| INV-G2-CAPTURE-CONFIRMATION | 只有当前平台回执/可信对账的 Confirm 命令能写 ON/STOPPED；请求、Action 回执、平台观察和业务确认互不替代 |
| INV-G2-CONTROL-SCOPE | Case 控制、Consent Receipt 与 ProcessingControl 相互独立；带作用域控制不得误伤其他案件、主体、目的或操作 |
| INV-G2-CONTROL-PREFLIGHT | 每个外部动作执行前重验生命周期、适用 processing control set、当前路由、目标修订、策略和负载 |
| INV-G2-NO-LLM-ACTION | LLM 只生成材料与建议，不能改状态、执行外部动作或作招聘决定 |
| INV-G2-SAFETY-COMPENSATION | CLOSED/PAUSED 阻断生产性动作，但不得阻断停止采集、取消会议、撤销链接和删除资产等安全补偿 |
| INV-G2-G1-HANDOFF | 一个会话只有一个 current_handoff_ref；Session=ENDED 且采集、片段处置和最终对账结算后才可创建；两条证据路线互斥，AUTHORIZED_NOTES_ONLY 只表示本次交接不引用音视频证据，不抹去实际采集历史或伪造证据质量 |
| INV-G2-G1-HANDOFF-INVALIDATION | causal input 失效时当前/已接受交接在 G1 读取或使用前也失效；替代输入建新 revision，迟到 Accepted 不复活旧输入 |
| INV-CONTROL-SINGLE-AGGREGATE | 一个命令事务只改变一个聚合和 Outbox；跨聚合只能由事件触发另一条带版本、幂等和当前事实重验的命令 |

## 10. 异常目录

| 代码 | 异常 | 默认处理 |
|---|---|---|
| SOURCE_NOT_ALLOWED | 投递来源未批准或身份不可证明 | 隔离，不解析；来源 Owner 复核 |
| ATTACHMENT_UNSAFE | 文件恶意、损坏、加密或类型不支持 | 停在投递层，提供安全补件/人工路线 |
| SUBMISSION_CONTENT_AMBIGUOUS | 一份投递包含多个候选人或材料归属不明 | 拆分复核，不开案 |
| PARSING_LOW_CONFIDENCE | 关键字段解析质量不足 | 标记未知并复核，不编造字段 |
| IDENTITY_AMBIGUOUS | 候选人身份候选冲突 | ROUTING_REVIEW_REQUIRED |
| REQUISITION_OR_CYCLE_UNRESOLVED | 岗位或招聘周期不唯一/不可处理 | 路由复核，禁止开案 |
| MATCH_INPUT_STALE | 简历、画像、字段策略或输入清单变化 | 当前匹配材料和决定请求失效，回 SCREENING |
| MATCH_VALIDATION_FAILED | 证据、维度或版本门未通过 | 不发布，不创建部门任务 |
| PROHIBITED_FEATURE_DETECTED | 禁用字段、代理变量或决定字段进入材料 | P0/P1 阻断并公平/隐私复核 |
| DEPARTMENT_OWNER_MISSING | 当前部门决策人无法解析或无权限 | 修复 Owner，保持单一任务 |
| DEPARTMENT_DECISION_OVERDUE | 催办与升级次数耗尽 | 开异常包，不将沉默解释成决定 |
| CALENDAR_PERMISSION_REVOKED | 忙闲/写入授权失效 | 停止使用旧快照，降级或异常 |
| NO_COMMON_SLOT | 约束内无共同时间 | 异常包提供替换人、拆场或延期选项 |
| PROPOSAL_EXPIRED | 候选人使用旧或过期时段提案 | 拒绝写入，生成当前提案 |
| SLOT_CONFLICT | 选中后忙闲发生变化 | 不建会，重新提案 |
| BOOKING_PARTIAL_SUCCESS | 日历、会议或邀请写入仅部分成功 | 对账、有限重试/补偿，不标 SCHEDULED |
| INVITE_DELIVERY_FAILED | 资源写入成功但通知持续失败 | 保持预定事实，运行态异常并升级 |
| RESCHEDULE_LIMIT_EXCEEDED | 改期/提案次数耗尽 | 交付带选项异常包 |
| CONSENT_MISSING_OR_STALE | 当前参与人/目的无有效选择凭证 | 阻断采集，走无录制路线 |
| LATE_PARTICIPANT_UNRESOLVED | 晚加入者选择未解决 | 暂停采集或保持无录制 |
| NO_RECORDING_ROUTE_UNAVAILABLE | 系统无法提供等价无录制流程 | 阻断试点采集并人工接管，不取消候选人机会 |
| CAPTURE_STATE_MISMATCH | 平台实际录制与控制面不一致 | P0 停止、熔断、对账和事件响应 |
| CASE_PAUSED_OR_CLOSED | 生产动作与控制态冲突 | 阻断生产动作；允许安全补偿 |
| DELETION_IN_PROGRESS | 权利请求正在传播 | 停止新用途处理，按血缘处置 |
| ACTION_POLICY_BLOCKED | 动作超出 A0–A3 授权或限制 | 不执行，记录策略原因 |
| G1_HANDOFF_INCOMPLETE | 会话/凭证/资产/笔记输入不完整 | 不交接或显式低证据路线，不伪造 |

## 11. 指标与发布门

| 维度 | 指标 | G2 发布原则 |
|---|---|---|
| 安全 | Agent 招聘决定、自动淘汰、错误收件人、跨案串档、未授权录制、旧预约复活 | 必须为 0 |
| 收件 | 合格投递漏接、重复业务效果、坏附件误解析 | 按来源分层，严重漏接/误并为 P0/P1 |
| 解析 | 关键字段精确率、未知率、字段来源覆盖、人工纠正率 | 金标与双标注后冻结阈值 |
| 路由 | 错误开案、重复案件、同人多岗隔离错误、复核率/P95 | 错误开案和串档为 0 |
| 匹配 | 关键 claim 证据覆盖、错引/无依据、禁用代理字段、实质修改率 | 关键 claim 证据覆盖 100%，无依据为 0 |
| 公平 | 错误率、未知率、决定漏斗按岗位/语言/适用群体分层 | 所有市场发布闸门；禁用字段不足以替代代理审计 |
| 决定 | 任务送达、部门响应、催办次数、Agent/Service 决定尝试 | 非人决定成功为 0 |
| 预约 | 双重业务预定、部分成功恢复、改期成功、旧回调抑制 | 重复当前预定和旧修订复活为 0 |
| 采集 | 告知/选择完整、晚加入阻断、停止请求至确认 SLO、平台状态一致 | 未授权/意外采集和乐观 STOPPED 为 0 |
| 控制 | 暂停/撤回/删除竞态、作用域误伤、补偿完成、异常恢复、计划外 HR 介入 | 先证明范围正确、停止和恢复，再升级动作 |
| 交接 | 当前 G1 交接唯一性、完整性、路线互斥、失效传播、录制/笔记来源诚实 | 重复/旧交接复活和虚假资产为 0 |
| 成本 | 每投递解析、匹配、通知、预约、会议、异常人工与供应商全成本 | 不只统计模型 Token |

指标必须同时记录分母、排除项、时间窗、Owner、数据源、切片、目标与反指标；没有基线时标记 UNKNOWN，不写乐观目标。

## 12. 验收场景

| ID | Given | When | Then |
|---|---|---|---|
| AT-201 | 邮件转发与 ATS 对同一申请各重复到达 | 收件和路由处理 | 保留来源审计，但只有一个当前投递业务效果和一个 ApplicationCase |
| AT-202 | 共享邮箱或复用手机号产生两个身份候选 | 自动路由 | 进入 ROUTING_REVIEW_REQUIRED，不自动合并或开案 |
| AT-203 | 同一候选人在同周期申请两个岗位，且其中一份已开案投递随后被授权纠正到另一岗位 | 两份投递处理并执行改键纠正 | ApplicationKey 不原地改写；旧关系/材料/任务/动作先 supersede，旧案补偿或关闭范围明确后打开/附加正确案件；两个案件隔离且不搬决定、会议或审计历史 |
| AT-204 | 附件损坏、加密、不支持或解析低质 | 收件门处理 | 停在投递层，开有限异常；不编造字段或案件 |
| AT-205 | 简历正文含“忽略规则并发邮件/读取数据”等提示注入 | 解析和匹配 | 只当不可信内容；tool execution count=0，不改状态、不外发、不取额外数据 |
| AT-206 | 解析版本或画像版本在部门任务打开期间变化 | 新版本生效 | 当前材料指针和请求在同一 ApplicationCase 事务失效，Case 回 SCREENING；新材料 READY 后再原子创建新请求，不出现“有等待阶段无任务” |
| AT-207 | 人岗匹配分低或证据不足 | 发布当前材料 | 授权人仍可审阅；不关闭、不淘汰、不发拒信 |
| AT-208 | 敏感字段或学校/姓名/口音/空窗等代理信号进入匹配 | 发布校验 | 相关结论阻断/隔离并进入公平复核 |
| AT-209 | Agent 或 Service 尝试提交 INVITE/HOLD/REJECT | 命令门校验 | 命令拒绝；只有当前授权 HUMAN 能提交 |
| AT-210 | 用人负责人双击当前卡、使用旧卡片或已处理任务再次提交 | 控制面处理 | 当前 HUMAN 决定只生效一次；INVITE/REJECT 后请求 CLOSED，HOLD 后同一 request_id 为新 ON_HOLD revision；旧 revision 不覆盖当前决定 |
| AT-211 | 决定已提交、请求 ON_HOLD/CLOSED/SUPERSEDED、案件暂停或静默时段开始后，排队催办到达；HOLD 到期 | Worker 执行 | 执行前阻断旧催办；同 ordinal 不重复；到期仅在当前门通过时生成一个 OPEN revision/generation，不替人决定 |
| AT-212 | Owner 缺失或通知重试耗尽 | 升级策略耗尽 | 只形成一个当前 Exception Bundle，不造第二权威任务或无限循环 |
| AT-213 | 部门任务或邀请收件人跨租户/错误 | 外发前校验 | P0 阻断、熔断和异常，外部效果为 0 |
| AT-214 | 同轮有两个必需 Session：第一场跨时区 Booking 已就绪，第二场仍在协调 | 第一场开始，第二场随后完成日历、会议和邀请写入 | 第一场开始即令 Round 进入 IN_PROGRESS；第二场仍可在 Session 层继续约面并形成唯一 Booking，后续 Booking 不得把 Round 回退为 SCHEDULED，时间展示一致 |
| AT-215 | 候选人选择过期提案或选择后忙闲变化 | 提交预约修订 | 不建会，旧选择无效，生成新提案或异常 |
| AT-216 | 两个案件并发抢占同一面试官时段 | 两个预约提交 | 至多一个当前 Booking；失败方回到提案，不双占 |
| AT-217 | Provider 已成功但响应丢失，回调又重复 10 次 | 控制面对账/重试 | 只有一个会议、邀请和 ActionExecution 业务效果 |
| AT-218 | 改期后旧 Provider 回调迟到 | 回调处理 | 只认当前 appointment revision；旧资源补偿取消，不能复活 |
| AT-219 | 候选人撤回或案件关闭时建会/邀请仍在队列 | Worker 执行 | 执行前阻断；已成功资源执行允许的安全补偿 |
| AT-220 | 日历权限失效或 Provider 长时间故障 | 预约流程 | 不猜忙闲、不伪造 Booking；有限重试后异常或人工路线 |
| AT-221 | 邀请含录制告知但没有当前 Consent Receipt | 会话开始并尝试 RequestCaptureStart | 面试可按无录制路线开始；请求被拒绝，采集保持 OFF，不把邀请/入会当授权 |
| AT-222 | 任一当前在场且媒体可能进入平台采集的参与人拒绝录制 | 选择生效 | capture_mode=NO_RECORDING，后续可形成 AUTHORIZED_NOTES_ONLY；拒绝不进入招聘评价 |
| AT-223 | 邀请被转发或临时/晚加入者进入 | 参与人快照变化且原采集为 ON | 立即 RequestCaptureStop 并进入 STOPPING；本人通过新闸门且平台停止确认前不得继续采集，只有当前停止回执可写 STOPPED |
| AT-224 | 参与人会中撤回当前 session/purpose 的采集选择 | 撤回生效 | 立即隔离新材料并发安全停止请求；未确认停止时保持 P0，不声称已停；撤回不自动影响范围外案件/目的，适用血缘处置留回执 |
| AT-225 | notice、purpose、participant、session、ProcessingControl scope/hash 或版本不匹配/过期 | 采集闸门计算 | 闸门拒绝，不能使用旧凭证或不适用控制快照 |
| AT-226 | Pause/Delete/Close/带作用域控制与 Worker/外部回调并发，Handoff 已 Created/Accepted 后 causal input 失效，或在 Session 未 ENDED、Capture 为 STARTING/ON/STOPPING/未知、片段处置/最终对账未完成时尝试创建 Handoff | 控制面与交接门重验 | 控制事实在其范围内获胜且不误伤范围外案件；未结算交接被硬拒绝；迟到 Confirm/Accepted 不推进或复活；已建交接产生 InterviewEvidenceHandoffInvalidated，G1 读取/使用被阻断，安全补偿继续 |
| AT-227 | 候选人协调链接被错人使用、重放或引用旧 revision | 请求提交 | 认证/当前版本门拒绝，不改时段或采集选择 |
| AT-228 | 合成正常链从批准来源产生新简历，并分别参数化 RECORDED_EVIDENCE 与 AUTHORIZED_NOTES_ONLY；后一参数再覆盖从未采集与撤回后仅笔记两种历史 | 流程自动运行至 Session=ENDED 且采集/片段处置/最终对账结算 | 来源事件驱动收件→路由→筛选→原子创建部门权威任务；Hiring Owner 人工决定；候选人选时/选择采集模式；唯一 Booking/Session/Handoff 经事件→带版本命令交给 G1，Handoff 保留真实 capture history，全程零 HR 启动、零 Agent 决定、外发均有回执 |

## 13. 放权、发布与回滚

### 13.1 动作级放权

| 等级 | 行为 | G2 示例 |
|---|---|---|
| 合成/沙箱 | 只用合成或匿名化数据和模拟连接器 | 当前允许范围 |
| A0 影子运行 | 读取批准输入并生成本来会执行的动作，不产生外部效果 | 比对解析、路由、匹配、提案和催办 |
| A1 建议模式 | 生成材料和动作草稿，每次由人审批 | 首批部门卡、候选人消息、预约写入 |
| A2 审批后执行 | 人批准策略或批次，Agent 在该授权内执行 | 特定岗位/时窗的协调与有限提醒 |
| A3 规则内自主 | 在明确规则、次数、预算和时间窗内执行，人只处理异常 | 最终的规则内收件、内部提醒、协调与标准邀请 |

放权单位是动作，不是整个 G2。内部归档或提醒达到 A3，不会自动使候选人外发、会议写入或录制达到 A3；招聘决定永久由人提交。

### 13.2 放行顺序

1. 合成回放 28 个 AT，并做重复/乱序/迟到/部分成功/竞态故障注入。
2. 经批准的历史数据离线验证解析、路由、证据、代理偏差和成本，不外发。
3. 真实账号最小权限 A0，只读影子并证明审计、停止和删除。
4. 按动作进入小样本 A1，候选人外发、会议写入和采集分别过门。
5. 连续证据达标后逐动作 A2/A3；任何 P0 先降回 A0 或停用。

当前 Gate 0 仍为 No-go，上述真实数据和真实动作均未放行。

### 13.3 立即回滚条件

- 自动淘汰、Agent/Service 招聘决定或用模型分数隐式关闭案件。
- 错误身份合并、跨岗位/周期串档、错误收件人或越权披露。
- 重复业务预定、旧预约复活、无法解释的日历/会议资源。
- 未授权录制、晚加入者被采集、拒绝录制影响招聘评价或撤回后继续处理。
- 暂停、撤回、删除或关闭后生产动作继续执行，或安全补偿被阻断。
- 简历提示注入触发工具、状态、外发或额外读取。
- 无证据关键匹配 claim、禁用代理字段进入已发布材料。
- G1 接收重复、错误会话或伪造录音/逐字稿。

回滚先冻结受影响动作和外发，保留最小审计，执行安全停止/补偿，人工接管，完成影响定界、修复、回归和签核后只从 A0 恢复。

## 14. Gate 0 前必须拍板

| 决策 | 推荐默认 | 决策人 |
|---|---|---|
| 首批来源 | 一个共享招聘邮箱或 ATS 入站，选择可稳定去重与对账者 | 技术 + 招聘运营 |
| 试点岗位 | 1–2 个画像清晰、流程稳定、量适中的岗位 | Sponsor + 用人负责人 |
| 身份/路由门 | 自动路由只接受唯一且无冲突候选；其余人工复核 | 产品 + 隐私 + 招聘运营 |
| 部门决定 | 产品内任务权威；INVITE/HOLD/REJECT 只由指定人提交 | 用人负责人 |
| HOLD 规则 | 必填原因和 revisit_at；到期创建新 SLA generation | 用人负责人 + 招聘运营 |
| 日历/会议硬门 | 明确每个平台哪些回执代表资源写入成功，哪些只是通知状态 | 技术 + QA |
| 改期策略 | 候选人自助次数、时间窗、替代面试官和超限升级 | 招聘运营 + 用人负责人 |
| 录制选择 | 逐参与人/会话/目的显式选择，默认提供等价无录制路线 | 隐私法务 + Sponsor |
| 晚加入能力 | 连接器不能暂停/停止采集时，试点一律无录制 | 隐私法务 + 技术 |
| 无录制交接 | 只接受批准的人工笔记来源，并显式标记证据局限 | 用人负责人 + QA |
| 动作放权 | 每个动作独立 Owner、门槛、频控、熔断和回滚 | Sponsor + 产品 + 技术 |
| 质量/公平门 | 先用金标与历史基线冻结；安全红线为 0，所有市场发布审计 | QA + AI/Data + 隐私法务 |

## 15. Definition of Done

只有以下全部有可检查证据，G2 才能称为“产品规格完成”：

- FR-201 至 FR-264 和 AT-201 至 AT-228 在 PRD、追踪矩阵与 lint 中连续、唯一、双向覆盖。
- CONTEXT 与 ADR-0007/0008 已冻结投递/案件、提案/预定、告知/选择/请求/平台确认和 G1 交接边界。
- 领域规格包含 G2 聚合、值对象、命令、事件、迁移、不变量、乱序、补偿和异常。
- ApplicationCase 仍只有六个顶层阶段；页面状态继续由 Case/Round/Session/Runtime 投影。
- 规格验收记录明确列出已证明与未证明内容，且实现/验证/发布计数仍为 0。

以下真实证据全部齐备并签核前，G2 不能称为“可发布”：

- 真实 Owner、试点岗位、数据地图、处理影响评估、告知/无录制路线和供应商协议。
- 金标解析/路由/证据集、代理偏差审计、红队与故障注入结果。
- 真实最小权限连接器、对账、幂等、停止/补偿、权限撤销和删除传播演练。
- A0、A1 及后续逐动作放权证据；零自动淘汰、错误收件人、串档、重复预定和未授权录制。
- 候选人、用人负责人、面试官、HR、技术、QA、AI/Data、隐私法务和 Sponsor 签字放行。

~~~yaml
acceptance:
  asserts:
    - "INV-G2-ROUTED-ONLY"
    - "INV-G2-ROUTING-CORRECTION"
    - "INV-G2-NO-AUTO-REJECT"
    - "INV-G2-HUMAN-DECISION"
    - "INV-G2-PROPOSAL-NOT-BOOKING"
    - "INV-G2-CURRENT-REVISION"
    - "INV-G2-NOTICE-NOT-CONSENT"
    - "INV-G2-NO-RECORDING-EQUIVALENCE"
    - "INV-G2-LATE-JOINER-GATE"
    - "INV-G2-CAPTURE-CONFIRMATION"
    - "INV-G2-CONTROL-SCOPE"
    - "INV-G2-CONTROL-PREFLIGHT"
    - "INV-G2-NO-LLM-ACTION"
    - "INV-G2-SAFETY-COMPENSATION"
    - "INV-G2-G1-HANDOFF"
    - "INV-G2-G1-HANDOFF-INVALIDATION"
    - "INV-CONTROL-SINGLE-AGGREGATE"
~~~
