# 招聘 Agent G1a MVP PRD：单轮面后证据闭环

版本：v0.2  
日期：2026-08-10  
状态：Gate 0 评审稿，尚未进入研发  
产品边界：已有录音或录制结束 → 转写 → 证据化评价草稿 → 面试官确认 → 轮次归档

相关材料：

- [产品落地总方案](./招聘Agent产品落地总方案.md)
- [推进看板](./招聘Agent推进看板.md)
- [领域语言](./CONTEXT.md)
- [ADR-0004：首个工程切片从面后证据闭环开始](./docs/adr/0004-first-engineering-slice-is-post-interview.md)

## 1. 要解决的问题

面试结束后，录音、纪要、评分卡和轮次结论彼此分散。面试官需要重新回忆并手写评价，HR 需要催确认、检查缺项和搬运结果；最终材料既慢，又难以回到原话证明。

本 MVP 要证明：

> 在不替代面试官判断的前提下，Agent 能把一次有合法录制材料的面试自动加工成逐项可溯源的评价草稿，并让面试官以更低成本完成确认，随后可靠地归入正确的申请案件和面试轮次。

## 2. 成功定义

G1a 成功不是“模型能写一篇报告”，而是以下条件同时成立：

- 正确的录音进入正确的申请案件与面试轮次。
- 只有具备有效录制授权记录的材料才会被处理。
- 每一条关键评价都能定位到逐字稿或其他允许使用的证据。
- Agent 不产生录用、淘汰或推进决定。
- 面试官能够看证据、修改、确认，并单独作出轮次决定。
- 重复回调、并发操作、模型失败和低质量录音不会造成重复报告或错误状态。
- 全过程可审计、可暂停、可人工接管、可删除。

北极星子指标：已确认评价闭环率。  
定义：在目标时限内完成“材料可用 → 草稿生成 → 指定确认人确认 → 轮次归档”的合格面试会话数 ÷ 进入本 MVP 处理范围的合格面试会话数。

## 3. 范围

### 3.1 本期包含

- 一个内部组织。
- 1–2 个试点岗位。
- 一个主会议/录音入口；人工上传作为降级入口。
- 产品内确认待办是权威入口；一个主 IM 或邮件入口只发送最小信息和安全深链。
- 中文面试；是否支持其他语言由 Gate 0 决定。
- 一个申请案件下的面试会话绑定。
- 录制授权校验、媒体接收、转写、说话人映射。
- 结构化纪要、评分卡维度评价草稿、证据定位。
- 面试官查看、修改、确认和独立提交轮次决定。
- 报告版本、确认记录、审计日志和异常包。
- 质量抽检、指标事件和单场成本记录。

### 3.2 本期不包含

- 简历收件、解析与人岗筛选的生产自动化。
- 自动约面、候选人改期和部门催办。
- 面试中实时追问或桌面端采集。
- 人脸、情绪、微表情、声纹性格或诚信推断。
- 自动录用、自动淘汰、自动推进或自动发送拒绝信。
- 自动修改岗位画像或评分卡。
- 面试官质检排名和管理惩罚。
- 多轮依赖、跨轮冲突处理和终面评估包；它们属于 G1b。
- 多租户、复杂 ATS 回写、OEM 和计费。
- 用本期数据训练通用模型。

### 3.3 进入处理范围的前置条件

一个面试会话只有同时满足以下条件才是“合格输入”：

1. 已绑定唯一申请案件、岗位需求和面试轮次。
2. 已锁定岗位画像版本与评分卡版本。
3. 已确定评价确认人；多人面试时有唯一确认责任人。
4. 每位被录制参与人的录制授权记录有效，且未撤回。
5. 录音可读取，文件类型、大小和完整性校验通过。
6. 案件未关闭、撤回、暂停或进入删除流程。

任一条件不满足，系统不得尝试“猜测后继续”，而应进入明确的异常或人工绑定流程。

## 4. 用户与权限

| 角色 | 可以做 | 不能做 |
|---|---|---|
| 面试官 | 查看自己参与事件的材料；修改/确认评价；提交轮次决定 | 查看无关岗位/候选人；修改画像；删除审计记录 |
| 评价确认人 | 对该会话的评价版本最终确认；处理多人意见 | 让 Agent 代替其作轮次决定 |
| 招聘 HR | 查看所负责案件状态；处理绑定/流程异常；催办或转派确认人 | 修改面试官评价而不留作者和原因；绕过授权处理录音 |
| 招聘运营管理员 | 配置岗位、评分卡、报告模板、提醒策略；抽检质量 | 查看超出授权范围的数据 |
| 隐私/安全管理员 | 查看授权、访问、导出、删除和安全审计 | 修改业务评价内容 |
| 质量标注员 | 在隔离的抽检任务中查看最小必要材料并标注错误 | 导出原始材料；查看非抽检样本 |
| Agent | 读取当前任务允许的材料；生成结构化草稿和建议 | 改状态、发外部消息、作招聘决定、扩大数据用途 |

权限必须同时考虑租户、部门、岗位、案件、面试会话、数据类型、处理目的和时间范围；只有角色名不足以构成授权。

## 5. 主流程

| 步骤 | 触发与系统动作 | 产物 | 状态要求 |
|---|---|---|---|
| 1 | 收到录制结束事件或人工上传 | 媒体接收任务 | 面试会话已结束或允许受控补传 |
| 2 | 校验事件唯一键、案件绑定、授权和案件状态 | 接收通过或异常包 | 未通过不得进入媒体处理 |
| 3 | 保存原始媒体指纹与最小必要元数据 | Media Artifact | 原件不可被后续重跑覆盖 |
| 4 | 转写并生成带时间戳的片段 | Transcript Version | 失败可重试；重试产生新运行记录 |
| 5 | 映射说话人并做质量检查 | Speaker Mapping + QC | 关键说话人不确定时转人工 |
| 6 | 提取证据单元，按评分卡生成纪要和评价草稿 | Evaluation Draft | 每个关键断言必须有证据引用 |
| 7 | 运行安全、禁用特征、证据一致性与完整性校验 | 可发布草稿或异常包 | 校验不通过不得发给面试官 |
| 8 | 控制面向指定确认人发送待确认卡片 | Confirmation Task | 写操作使用唯一动作键 |
| 9 | 面试官查看证据、修改维度、补充说明 | Evaluation Version | 保存作者、差异、时间和理由 |
| 10 | 面试官确认评价，并单独提交轮次决定 | Confirmed Evaluation + Round Decision | 决定字段不由 Agent 预填为默认值 |
| 11 | 控制面归档该轮并更新案件材料完整性 | Round Archived | 必需材料齐全才可结束轮次 |

## 6. 产品界面

### 6.1 面试官待确认卡片

必须显示：

- 候选人、岗位、轮次、面试时间和确认截止时间。
- “AI 生成草稿，需面试官确认”的显著标记。
- 三项摘要：已验证事实、仍待验证点、低置信/冲突。
- 每个评分卡维度的草稿结论、置信提示和证据数量。
- 查看完整报告、稍后处理、转交（需权限）、报告问题。

不能显示：

- Agent 自动推荐的录用/淘汰结论。
- 未被评分卡定义的性格、情绪、健康、婚育等推断。
- 对候选人的单一“综合人格分”。

### 6.2 评价工作台

每个维度需要：

- 维度定义与行为锚点。
- Agent 草稿与“为何生成”。
- 可点击的逐字稿时间戳。
- 支持标记证据正确、错引、缺失或与结论相反。
- 面试官评分/结论、文字修改和“不足以判断”选项。
- 保存草稿、确认评价和报告问题。

轮次决定必须是独立区域：

- 进入下一轮、保留、淘汰或其他组织批准的选项。
- 必须由面试官主动选择，不能由评价分数自动预选。
- 淘汰或推进可要求选择业务原因，但原因不得使用禁用特征。

### 6.3 HR 案件视图

显示：

- 当前业务阶段和运行状态。
- 媒体接收、转写、生成、发布、确认的时间线。
- 当前责任人、等待时长、提醒次数和下一动作。
- 评价版本、确认状态和轮次完整性。
- 异常包、已尝试动作和恢复入口。

HR 可以：

- 纠正案件/轮次绑定。
- 指定或更换确认人。
- 暂停、恢复、转人工上传、重试允许重试的任务。
- 发起删除/撤回处理。

HR 不能：

- 无痕修改面试官评价。
- 强行将未确认评价标为已确认。
- 在授权缺失时绕过录制处理闸门。

### 6.4 质量抽检视图

- 使用抽样任务而不是开放搜索全部面试。
- 并排显示源证据、Agent 草稿、面试官修改和错误标签。
- 记录证据准确、覆盖、说话人、禁用特征、严重度和根因。
- 访问到期自动回收；禁止批量导出原始媒体。

## 7. 功能需求

### 7.1 绑定、接收与授权

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-001 | 系统必须用外部事件 ID + 资源 ID + 租户生成接收幂等键 | 相同回调重复 10 次只创建一个媒体任务 |
| FR-002 | 每个媒体必须绑定唯一面试会话、轮次和申请案件 | 多匹配或无匹配时停止并创建异常包 |
| FR-003 | 处理前必须校验所有需授权参与人的录制授权记录 | 缺失、撤回或文本版本无效时不得转写 |
| FR-004 | 人工上传必须记录上传人、来源、处理目的和案件绑定 | 上传不能绕过 FR-002/003 |
| FR-005 | 案件处于撤回、关闭、暂停或删除中时禁止新处理 | 队列中旧任务执行前也要重新校验 |

### 7.2 转写与证据

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-006 | 转写输出必须带媒体版本、片段时间戳和说话人标识 | 任一证据可回放到对应时间范围 |
| FR-007 | 系统必须记录转写与说话人质量信号 | 低于阈值时转人工或降级为纪要不可评估 |
| FR-008 | 证据单元必须保留原文、来源、时间范围和提取版本 | 重跑不覆盖旧证据 |
| FR-009 | 候选人、面试官和未知说话人不能在低置信时被强行映射 | 关键错配可以阻断报告发布 |
| FR-010 | 原始媒体、逐字稿和评价必须按数据分级独立控制访问 | 有评价权限不自动获得原始媒体下载权 |

### 7.3 评价生成与校验

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-011 | 评价必须使用案件锁定的画像和评分卡版本 | 新画像发布不影响在途案件 |
| FR-012 | 每条关键评价必须引用至少一个证据单元 | 无证据关键结论不得进入可发布版本 |
| FR-013 | 草稿必须区分事实、证据支持的判断、存疑和信息不足 | 信息不足不能被补写成负面结论 |
| FR-014 | 系统必须检测禁用特征及明显代理变量的使用 | 命中时阻断相关结论并进入质量复核 |
| FR-015 | Agent 输出结构不得包含录用/淘汰/推进决定字段 | Prompt、Schema 和发布校验三层阻断 |
| FR-016 | 安全校验失败时不得静默删除后继续发布 | 创建可追踪异常并保留失败原因 |

### 7.4 确认、决定与归档

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-017 | 每个会话必须有唯一评价确认责任人 | 多人面试时其他人可评论但不能造成多个生效版本 |
| FR-018 | 每次修改必须产生新版本并记录差异、作者、时间和原因 | 生成版和确认版都可审计 |
| FR-019 | 评价确认和轮次决定是两个独立动作 | 确认评价不会自动推进案件 |
| FR-020 | 并发确认必须使用版本校验 | 后提交者看到冲突，不覆盖先确认结果 |
| FR-021 | 必需评价未确认时不能把轮次归档为完成 | 授权豁免必须记录人、原因和时间 |
| FR-022 | 轮次归档必须输出不可变的确认版、决定版和证据版本引用，供 G1b 后续汇总 | G1a 不编译终面评估包，也不把草稿伪装成确认结果 |

### 7.5 提醒、异常与人工接管

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-023 | 提醒按岗位策略、静默时段、次数上限和升级链执行 | 重试不能重复发送同一次提醒 |
| FR-024 | 异常包必须包含事实、风险、已尝试动作、选项、建议、责任人和截止时间 | HR 无需重新查找上下文即可处理 |
| FR-025 | 每类任务必须定义可重试、不可重试和需人工恢复 | 模型失败与授权缺失不能使用同一恢复策略 |
| FR-026 | 支持案件、岗位和租户三级暂停/熔断 | 暂停后未执行的外发动作全部失效 |
| FR-027 | 人工接管和恢复必须记录接管范围与恢复点 | 恢复不能从头重复外发 |

### 7.6 审计、权利与删除

| ID | 需求 | 验收要点 |
|---|---|---|
| FR-028 | 记录每次读取、生成、修改、确认、发送、导出和删除 | 可按案件和人员生成完整审计时间线 |
| FR-029 | 保存模型、Prompt、评分卡、策略和模板版本 | 能解释某一版本报告为何产生 |
| FR-030 | 撤回同意或删除请求必须传播到媒体、逐字稿、派生证据和允许删除的报告副本 | 有数据血缘和完成回执 |
| FR-031 | 删除进行中必须停止新的生成、提醒和分发 | 队列任务在执行前重验删除状态 |
| FR-032 | 数据默认不得进入模型训练或跨目的分析 | 任何例外需要独立批准和可证明授权 |

## 8. 业务规则与不变量

1. 一个申请案件可有多个面试轮次，一个轮次可有多个面试会话。
2. 一个面试会话只有一个当前生效的已确认评价，但保留全部历史版本。
3. 一份媒体只能绑定一个面试会话；需要复用时创建显式引用，不复制业务归属。
4. 在途案件继续使用锁定的画像/评分卡版本。
5. 没有有效录制授权记录，不得进入转写和评价生成。
6. 低质量材料可以生成“不可可靠评估”的结果，不能被模型补全。
7. 评价草稿不能改变案件业务阶段。
8. 轮次决定必须由有权限的人主动提交。
9. G1a 只判断当前轮次是否可归档，不计算全案终面评估是否就绪。
10. 撤回、暂停和删除优先于队列中尚未执行的所有动作。
11. 重跑产生新版本，不覆盖生成版、人工修改或已确认版。
12. 重试不得产生重复提醒、重复卡片或重复归档。

## 9. 异常目录

| 代码 | 异常 | 默认处理 |
|---|---|---|
| BINDING_NOT_FOUND | 录音无法绑定案件/轮次 | HR 人工绑定 |
| BINDING_AMBIGUOUS | 同一录音匹配多个事件 | 阻断并展示候选项 |
| CONSENT_MISSING | 参与人授权缺失 | 不处理，转无录制人工路线 |
| CONSENT_REVOKED | 处理前或处理中撤回 | 停止后续任务并触发数据处置 |
| MEDIA_UNREADABLE | 文件损坏或格式不支持 | 请求重新上传 |
| AUDIO_LOW_QUALITY | 音质不足以可靠转写/评估 | 人工判断只出纪要或放弃自动评估 |
| SPEAKER_UNCERTAIN | 关键发言人映射不确定 | 人工校正映射 |
| SCORECARD_MISSING | 未锁定评分卡版本 | 业务管理员修复配置 |
| EVIDENCE_VALIDATION_FAILED | 关键结论缺证据或错引 | 阻断发布并进入质量复核 |
| PROHIBITED_FEATURE_DETECTED | 评价使用禁用特征/代理变量 | 删除相关推断，质量与合规复核 |
| MODEL_UNAVAILABLE | 模型服务不可用 | 有限重试后进入人工/延迟队列 |
| CONFIRMATION_OVERDUE | 确认人超时 | 按策略提醒、升级或转派 |
| VERSION_CONFLICT | 多人并发修改 | 要求基于最新版本重新提交 |
| CASE_PAUSED_OR_CLOSED | 案件状态不允许继续 | 取消未执行任务 |
| DELETION_IN_PROGRESS | 权利请求处理中 | 停止处理并等待删除完成 |

## 10. 事件与指标采集

每个领域事件至少包含：event_id、occurred_at、tenant_id、application_case_id、interview_round_id、interview_session_id、actor_type、actor_id、correlation_id、causation_id、policy_version；不得在分析事件中复制不必要的简历或逐字稿正文。

| 事件 | 用途 |
|---|---|
| RecordingAvailable | 计算接收量和来源成功率 |
| RecordingRejected | 统计授权、绑定、格式等拒绝原因 |
| TranscriptionStarted / Completed / Failed | 时效、可靠性和成本 |
| SpeakerMappingNeedsReview | 说话人质量 |
| EvaluationDraftGenerated | 草稿生成时效 |
| EvaluationDraftBlocked | 证据/合规校验失败率 |
| ConfirmationTaskDelivered | 送达与提醒基线 |
| EvaluationEdited | 实质修改率、错误类型 |
| EvaluationConfirmed | 确认率与确认时长 |
| RoundDecisionSubmitted | 决定完整性，不用作模型正确标签 |
| RoundArchived | 闭环率 |
| ExceptionRaised / Resolved | 异常率、恢复率和恢复时长 |
| ConsentWithdrawn | 权利请求与停止处理验证 |
| DataDeletionCompleted | 删除 SLA |
| ExternalActionSuppressed | 暂停、幂等和策略阻断效果 |

## 11. 质量与运行指标

以下门槛在 Gate 0 基线后冻结；安全红线不等待基线。

| 维度 | 指标 | G1a 发布原则 |
|---|---|---|
| 安全 | 未授权处理、越权访问、错误收件人、自动决定 | 必须为 0 |
| 证据 | 关键结论证据覆盖率 | 必须为 100% |
| 证据 | 关键无依据/错引结论数 | 必须为 0 才可升到实时生产 |
| 可靠性 | 重复卡片、重复归档、重复提醒 | 必须为 0 |
| 采用 | 已确认评价闭环率 | 先建立基线，再设置放权门槛 |
| 时效 | 录音可用到草稿 P50/P95 | 10 分钟作为待验证目标，不冒充当前能力 |
| 质量 | 说话人正确率、证据准确率、实质修改率 | 按岗位/语言/设备分层报告 |
| 协作 | 草稿到确认时长、超时率、转派率 | 识别产品与组织瓶颈 |
| 成本 | 转写、模型、存储、人工复核的单场成本 | 全成本口径，不只算模型 Token |
| 公平 | 分组转写/证据/推荐误差 | 所有市场发布闸门；样本不足必须披露 |

“与面试官终评一致率”只作辅助分析，因为面试官本身可能不一致；G1a 的主质量信号是证据正确、覆盖完整、修改类型和错误严重度。

## 12. 验收场景

| ID | Given | When | Then |
|---|---|---|---|
| AT-001 | 合格案件、有效授权、可用录音 | 收到录制结束事件 | 只创建一个处理任务并最终生成可溯源草稿 |
| AT-002 | 同一回调重复到达 10 次 | 控制面接收 | 仅一个媒体和一个草稿，其他标为重复 |
| AT-003 | 候选人授权缺失 | 录音到达 | 不转写、不生成评价，创建 CONSENT_MISSING 异常 |
| AT-004 | 转写前候选人撤回授权 | 队列任务将执行 | 任务停止，不产生新派生数据 |
| AT-005 | 关键说话人低置信 | 生成前校验 | 不发布评分评价，进入人工说话人映射 |
| AT-006 | 模型输出一个无证据负面结论 | 发布校验 | 草稿被阻断并记录 EVIDENCE_VALIDATION_FAILED |
| AT-007 | 转写提及婚育/健康等无关信息 | 评价生成 | 相关内容不进入评价，命中合规复核 |
| AT-008 | 两名用户编辑同一版本 | 后者提交 | 收到版本冲突，不覆盖先提交结果 |
| AT-009 | 面试官确认评价但未作轮次决定 | 点击确认 | 评价生效，案件不自动推进 |
| AT-010 | 当前轮已完成，但同案其他轮次尚未完成 | 归档当前轮 | 当前轮成功归档；不生成终面评估包或全案就绪事件 |
| AT-011 | 案件已暂停但提醒在队列中 | 到计划发送时间 | 发送前状态校验阻断提醒 |
| AT-012 | 模型超时且达到重试上限 | 最后一次失败 | 创建异常包，可人工接管且不重复处理媒体 |
| AT-013 | 删除请求覆盖媒体及派生数据 | 删除执行 | 停止生成，按血缘删除并形成完成回执 |
| AT-014 | 新评分卡在面试后发布 | 旧案件重跑 | 仍使用案件锁定版本，除非有授权迁移记录 |
| AT-015 | 恶意提示写在逐字稿中 | Agent 生成 | 被当作候选人内容，不触发工具或改变系统指令 |

## 13. 发布、放权与回滚

### 13.1 发布阶梯

| 阶段 | 行为 | 放行要求 |
|---|---|---|
| 离线回放 | 对经批准历史样本生成，不发给业务用户 | 金标集、错误分类、P0 红队和成本基线可用 |
| A0 影子 | 处理真实新录音但不向面试官发送 | 连续样本无 P0，证据/绑定/授权闸门通过 |
| A1 建议 | 向小范围面试官发草稿，所有后续动作由人确认 | 零错误收件人、零重复外发、可一键暂停/接管 |
| A2 扩围 | 对试点岗位自动创建确认任务与规则内提醒 | 达到冻结的质量、采用、时效与异常恢复门槛 |

招聘决定永久不开放 A3；G1a 最多把通过证据门的非决定性动作扩到 A2，也不把整个招聘链声明为自主托管。

### 13.2 回滚触发

任一条件立即暂停相应动作：

- 未授权录制处理、跨权限访问或数据泄露。
- 错误候选人/岗位绑定并向面试官分发。
- 自动生成或发送招聘决定。
- 关键无依据结论进入已发布草稿。
- 重复外发、重复归档或无法停止队列动作。
- 删除/撤回后仍继续处理。
- 发现模型供应商违规留存或训练使用。

回滚后：

1. 冻结受影响动作和在途外发。
2. 保留事件与审计证据，不扩散原始内容。
3. 确认影响案件和人员。
4. 人工接管并按事件响应流程通知。
5. 修复、离线回放、重新过门后才恢复。

## 14. Gate 0 前必须拍板

| 决策 | 推荐默认 | 决策人 |
|---|---|---|
| 试点岗位 | 1–2 个高频、流程标准、用人经理承诺的岗位 | Sponsor + 招聘运营 |
| 主录音入口 | 只选一个平台，人工上传作兜底 | 技术负责人 |
| 确认入口 | 一个主 IM 或 Web，不同时做多平台 | 产品 + 技术 |
| 多人面试确认模式 | 每个面试会话一个确认责任人，其他人为协作者 | 招聘运营 |
| 录制不同意路径 | 转无录制人工记录，不影响候选人结果 | 隐私法务 + HR |
| 评分卡版本 | 在事件开始前锁定；在途案件不自动迁移 | 用人部门负责人 |
| 提醒时点与上限 | 按真实流程基线后冻结 | 招聘运营 |
| 保存期限 | 按目的最短、法务批准，不在 PRD 中拍脑袋给天数 | 隐私法务 |
| 模型与数据区域 | 优先境内、默认不训练，跨境需单独论证 | 安全/法务 + 技术 |
| G1a 质量门槛 | Gate 0 用金标集和历史基线冻结 | 产品 + AI + QA |

## 15. Definition of Done

只有以下全部有可检查证据，G1a 才算完成：

- FR-001 至 FR-032 有实现、测试和责任人。
- AT-001 至 AT-015 自动化或可重复人工验收通过。
- 合法真实样本完成离线、A0 和 A1 阶段验证。
- P0 红队通过，未授权处理、错误收件人、自动决定均为 0。
- 证据质量按岗位/语言/设备分层报告，不用总体平均掩盖问题。
- 暂停、熔断、人工接管、恢复和删除完成真实演练。
- 事件、指标、成本和异常能够在案件级追踪。
- 招聘运营、技术、AI/数据、QA、隐私法务和试点业务 Owner 完成签字放行。

~~~yaml
acceptance:
  asserts:
    - "scope_is_post_interview_evidence_loop"
    - "agent_cannot_change_case_state_or_make_hiring_decision"
    - "valid_consent_is_required_before_processing"
    - "every_critical_evaluation_claim_has_source_evidence"
    - "evaluation_confirmation_and_round_decision_are_separate"
    - "duplicate_events_and_retries_are_idempotent"
    - "pause_withdrawal_and_deletion_stop_pending_actions"
    - "release_progresses_offline_to_A0_to_A1_to_A2"
    - "rollback_triggers_are_explicit"
~~~
