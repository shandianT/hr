# 招聘 Agent 控制塔逻辑原型验收记录

日期：2026-08-10  
原型：[招聘Agent控制塔_逻辑原型.html](./招聘Agent控制塔_逻辑原型.html)  
SHA-256：`4b709a29647d6a4fe12ec5e89c5c55a1798ac1f20bf1160769c0c022b05a078d`  
数据范围：完全合成  
发布结论：仅通过逻辑原型验收；真实数据、A0/A1、外部发送和工程发布仍为 No-go

## 1. 本次验证的问题

> 产品、招聘 HR 和用人经理能否只看一个案件页面，说清案件走到哪、为何停住、下一步由谁完成，以及 Agent 哪些事不能做？

原型验证的是状态、命令门、页面投影、幂等、异常、暂停与撤回的产品逻辑，不验证正式视觉、持久化、并发、连接器、模型或合规。

## 2. 自动可重复场景结果

每一步都展示 `expected / actual / PASS|FAIL`；非 APPLIED 命令若改变聚合版本，会被原型自身判为失败。

| 场景 | 检查数 | 结果 | 关键证明 |
|---|---:|---|---|
| 完整正常链 | 26 | PASS | 从 ResumeSubmission 跑完两轮，到 Case=`FINAL_ASSESSMENT_READY`；未自动作最终决定 |
| 路由冲突 | 7 | PASS | 未唯一路由前无 ApplicationCase；Agent 自动选路由被拒绝 |
| 拒绝录制 | 7 | PASS | 录制保持关闭；招聘经等价无录制与人工笔记路线继续 |
| 重复与乱序 | 6 | PASS | 旧 revision 忽略；同 source event 重放不增加聚合版本 |
| 评价确认 ≠ 轮次决定 | 5 | PASS | 两次人类动作独立；Agent 代签和代决策均被拒绝 |
| 暂停与恢复 | 7 | PASS | Case/Round 不变；旧队列动作失效；恢复从原检查点继续 |
| 候选人撤回 | 5 | PASS | 关闭优先；提醒阻断；仅允许受控会议取消补偿 |
| 合计 | 63 | **全部 PASS** | 无场景失败 |

浏览器运行日志：0 个 error/warning。  
JavaScript 语法检查：PASS。  
本地 HTTP 加载：PASS。  
外部 URL、脚本、字体、请求与连接器引用：0。

## 3. 已证明的产品逻辑

1. 简历投递和申请案件边界可以被非研发人员观察；未形成人、岗位、周期唯一键时不能开案。
2. ApplicationCase 只使用六个业务阶段；待约面、已排期、待确认等由 RoundState 推导。
3. RuntimeEnvelope 与业务状态正交，PAUSED 不覆盖业务阶段或异常事实。
4. 所有状态变化只来自业务命令，没有直接编辑 CaseStage、RoundState 或页面状态的入口。
5. LLM/Agent 不能直接改状态、确认评价或形成部门/轮次/最终招聘决定。
6. 重复来源事件不重复产生业务效果；旧预约 revision 和乱序回调不让状态回退。
7. 暂停和候选人撤回优先于待执行提醒；恢复不复活旧 generation 动作。
8. 异常包保留事实、风险、尝试、选项、Owner、截止时间和恢复点；恢复不从流程头开始。
9. G1a 的轮次确认/归档、G1b 的多轮终面包、G2 的投递/匹配/约面在同一领域模型中可演示，同时保持阶段放行边界。

## 4. 尚未证明

- 招聘 HR、用人经理、面试官和法务尚未实际试用并签核交互语义。
- reducer 是浏览器内存演示，不是生产工作流引擎或正式 API 契约实现。
- 未验证数据库事务、并发、Outbox、恢复、权限、审计防篡改和跨租户隔离。
- 未连接邮箱、IM、日历、会议、ATS、转写或模型，也未证明任何真实权限和可靠性。
- 未使用真实简历、录音或评价；不证明模型质量、候选人体验、公平性或合法数据路径。
- 63 个确定性场景不是 QA 回归集，不能替代 G1a PRD 的工程测试和真实 Gate 证据。

## 5. 下一轮产品评审方法

让招聘运营、技术、用人经理和隐私法务分别完成以下操作并记录异议：

1. 不看规格，独立解释四轴状态和下一责任人。
2. 各自跑一次“路由冲突”“拒绝录制”“确认 ≠ 决定”“暂停与恢复”。
3. 指出任何“不应该允许”或“应该允许但被拒绝”的动作。
4. 对异常包判断是否已经足以直接作决定，而无需另查上下文。
5. 将确认的结论写回领域规格和 G1a/G1b/G2 Backlog；原型 HTML 不作为生产代码延续。
