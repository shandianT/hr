---
status: accepted
---

# 确定性控制面拥有状态和外部动作

Agent 负责理解非结构化材料、生成草稿和提出结构化动作建议；申请案件状态、计时器、权限、审批、重试、幂等和所有改变外部世界的动作由确定性控制面执行。相比一个可自由调用邮箱、日历和消息工具的“大 Agent”，该选择牺牲部分开发表面速度，以换取可审计、可重放、可熔断和不重复外发的生产可靠性。

## Consequences

- 模型不能直接修改案件状态、发送消息或创建会议。
- 每个外部动作在执行前重新校验案件状态、收件人、策略版本和权限。
- 模型不可用时，案件仍可暂停、人工接管、恢复和回放。
